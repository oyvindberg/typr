/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currencyrate

import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class CurrencyrateRepoMock(
  toRow: CurrencyrateRowUnsaved => CurrencyrateRow,
  map: HashMap[CurrencyrateId, CurrencyrateRow] = new HashMap[CurrencyrateId, CurrencyrateRow]()
) extends CurrencyrateRepo {
  def delete: DeleteBuilder[CurrencyrateFields, CurrencyrateRow] = {
    new DeleteBuilderMock(
      CurrencyrateFields.structure,
      () => new ArrayList(map.values()),
      DeleteParams.empty(),
      row => row.currencyrateid,
      id => map.remove(id): @scala.annotation.nowarn
    )
  }

  def deleteById(currencyrateid: CurrencyrateId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(currencyrateid)).isPresent()

  def deleteByIds(currencyrateids: Array[CurrencyrateId])(using c: Connection): Integer = {
    var count = 0
    currencyrateids.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    count
  }

  def insert(unsaved: CurrencyrateRow)(using c: Connection): CurrencyrateRow = {
    if (map.containsKey(unsaved.currencyrateid)) {
      throw new RuntimeException(s"id $unsaved.currencyrateid already exists")
    }
    map.put(unsaved.currencyrateid, unsaved): @scala.annotation.nowarn
    unsaved
  }

  def insert(unsaved: CurrencyrateRowUnsaved)(using c: Connection): CurrencyrateRow = insert(toRow(unsaved))(using c)

  def insertStreaming(
    unsaved: java.util.Iterator[CurrencyrateRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: java.util.Iterator[CurrencyrateRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    count
  }

  def select: SelectBuilder[CurrencyrateFields, CurrencyrateRow] = new SelectBuilderMock(CurrencyrateFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  def selectAll(using c: Connection): java.util.List[CurrencyrateRow] = new ArrayList(map.values())

  def selectById(currencyrateid: CurrencyrateId)(using c: Connection): Optional[CurrencyrateRow] = Optional.ofNullable(map.get(currencyrateid))

  def selectByIds(currencyrateids: Array[CurrencyrateId])(using c: Connection): java.util.List[CurrencyrateRow] = {
    val result = new ArrayList[CurrencyrateRow]()
    currencyrateids.foreach { id => val opt = Optional.ofNullable(map.get(id))
    if (opt.isPresent()) result.add(opt.get()): @scala.annotation.nowarn }
    result
  }

  def selectByIdsTracked(currencyrateids: Array[CurrencyrateId])(using c: Connection): java.util.Map[CurrencyrateId, CurrencyrateRow] = selectByIds(currencyrateids)(using c).stream().collect(Collectors.toMap((row: adventureworks.sales.currencyrate.CurrencyrateRow) => row.currencyrateid, Function.identity()))

  def update: UpdateBuilder[CurrencyrateFields, CurrencyrateRow] = {
    new UpdateBuilderMock(
      CurrencyrateFields.structure,
      () => new ArrayList(map.values()),
      UpdateParams.empty(),
      row => row
    )
  }

  def update(row: CurrencyrateRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.currencyrateid)).filter(oldRow => !oldRow.equals(row)).isPresent()
    if (shouldUpdate) {
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
    }
    shouldUpdate
  }

  def upsert(unsaved: CurrencyrateRow)(using c: Connection): CurrencyrateRow = {
    map.put(unsaved.currencyrateid, unsaved): @scala.annotation.nowarn
    unsaved
  }

  def upsertBatch(unsaved: java.util.Iterator[CurrencyrateRow])(using c: Connection): java.util.List[CurrencyrateRow] = {
    val result = new ArrayList[CurrencyrateRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: java.util.Iterator[CurrencyrateRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      count = count + 1
    }
    count
  }
}