/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.password

import adventureworks.person.businessentity.BusinessentityId
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class PasswordRepoMock(
  toRow: PasswordRowUnsaved => PasswordRow,
  map: HashMap[BusinessentityId, PasswordRow] = new HashMap[BusinessentityId, PasswordRow]()
) extends PasswordRepo {
  def delete: DeleteBuilder[PasswordFields, PasswordRow] = {
    new DeleteBuilderMock(
      PasswordFields.structure,
      () => new ArrayList(map.values()),
      DeleteParams.empty(),
      row => row.businessentityid,
      id => map.remove(id): @scala.annotation.nowarn
    )
  }

  def deleteById(businessentityid: BusinessentityId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(businessentityid)).isPresent()

  def deleteByIds(businessentityids: Array[BusinessentityId])(using c: Connection): Integer = {
    var count = 0
    businessentityids.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    count
  }

  def insert(unsaved: PasswordRow)(using c: Connection): PasswordRow = {
    if (map.containsKey(unsaved.businessentityid)) {
      throw new RuntimeException(s"id $unsaved.businessentityid already exists")
    }
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    unsaved
  }

  def insert(unsaved: PasswordRowUnsaved)(using c: Connection): PasswordRow = insert(toRow(unsaved))(using c)

  def insertStreaming(
    unsaved: java.util.Iterator[PasswordRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: java.util.Iterator[PasswordRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    count
  }

  def select: SelectBuilder[PasswordFields, PasswordRow] = new SelectBuilderMock(PasswordFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  def selectAll(using c: Connection): java.util.List[PasswordRow] = new ArrayList(map.values())

  def selectById(businessentityid: BusinessentityId)(using c: Connection): Optional[PasswordRow] = Optional.ofNullable(map.get(businessentityid))

  def selectByIds(businessentityids: Array[BusinessentityId])(using c: Connection): java.util.List[PasswordRow] = {
    val result = new ArrayList[PasswordRow]()
    businessentityids.foreach { id => val opt = Optional.ofNullable(map.get(id))
    if (opt.isPresent()) result.add(opt.get()): @scala.annotation.nowarn }
    result
  }

  def selectByIdsTracked(businessentityids: Array[BusinessentityId])(using c: Connection): java.util.Map[BusinessentityId, PasswordRow] = selectByIds(businessentityids)(using c).stream().collect(Collectors.toMap((row: adventureworks.person.password.PasswordRow) => row.businessentityid, Function.identity()))

  def update: UpdateBuilder[PasswordFields, PasswordRow] = {
    new UpdateBuilderMock(
      PasswordFields.structure,
      () => new ArrayList(map.values()),
      UpdateParams.empty(),
      row => row
    )
  }

  def update(row: PasswordRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.businessentityid)).filter(oldRow => !oldRow.equals(row)).isPresent()
    if (shouldUpdate) {
      map.put(row.businessentityid, row): @scala.annotation.nowarn
    }
    shouldUpdate
  }

  def upsert(unsaved: PasswordRow)(using c: Connection): PasswordRow = {
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    unsaved
  }

  def upsertBatch(unsaved: java.util.Iterator[PasswordRow])(using c: Connection): java.util.List[PasswordRow] = {
    val result = new ArrayList[PasswordRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: java.util.Iterator[PasswordRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      count = count + 1
    }
    count
  }
}