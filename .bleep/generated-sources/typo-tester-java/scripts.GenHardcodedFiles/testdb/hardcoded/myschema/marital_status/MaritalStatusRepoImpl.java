/**
 * File automatically generated by `typo` for its own test suite.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN
 */
package testdb.hardcoded.myschema.marital_status;

import java.sql.Connection;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.hardcoded.myschema.marital_status.MaritalStatusFieldValue.id;
import typo.runtime.Fragment;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public record MaritalStatusRepoImpl() implements MaritalStatusRepo {
  public Boolean deleteById(
    MaritalStatusId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "myschema"."marital_status" where "id" = 
      """),
      MaritalStatusId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };
  public Integer deleteByIds(
    MaritalStatusId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "myschema"."marital_status"
                  where "id" = ANY("""),
               MaritalStatusId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };
  public MaritalStatusRow insert(
    MaritalStatusRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "myschema"."marital_status"("id")
         values ("""),
      MaritalStatusId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("""
         ::int8)
         returning "id"
      """)
    )
      .updateReturning(MaritalStatusRow._rowParser.exactlyOne()).runUnchecked(c);
  };
  public Long insertStreaming(
    Iterator<MaritalStatusRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "myschema"."marital_status"("id") FROM STDIN
    """), batchSize, unsaved, c, MaritalStatusRow.pgText);
  };
  public List<MaritalStatusRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id"
       from "myschema"."marital_status"
    """)).as(MaritalStatusRow._rowParser.all()).runUnchecked(c);
  };
  public List<MaritalStatusRow> selectByFieldValues(
    List<MaritalStatusFieldValue<?>> fieldValues,
    Connection c
  ) {
    Fragment where = Fragment.whereAnd(
        fieldValues.stream().map(
          fv -> switch (fv) {
                  case id x -> interpolate(
                    typo.runtime.Fragment.lit("""
                    "id" = 
                    """),
                    MaritalStatusId.pgType.encode(x.value()),
                    typo.runtime.Fragment.lit("")
                  );
                }
        ).toList()
      );;
    return interpolate(
             typo.runtime.Fragment.lit("""
             select "id" from "myschema"."marital_status" 
             """),
             where,
             typo.runtime.Fragment.lit("")
           ).as(MaritalStatusRow._rowParser.all()).runUnchecked(c);
  };
  public Optional<MaritalStatusRow> selectById(
    MaritalStatusId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id"
         from "myschema"."marital_status"
         where "id" = """),
      MaritalStatusId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(MaritalStatusRow._rowParser.first()).runUnchecked(c);
  };
  public List<MaritalStatusRow> selectByIds(
    MaritalStatusId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id"
         from "myschema"."marital_status"
         where "id" = ANY("""),
      MaritalStatusId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(MaritalStatusRow._rowParser.all()).runUnchecked(c);
  };
  public Map<MaritalStatusId, MaritalStatusRow> selectByIdsTracked(
    MaritalStatusId[] ids,
    Connection c
  ) {
    Map<MaritalStatusId, MaritalStatusRow> ret = new HashMap<>();;
      for (var row : selectByIds(ids, c)) {
          ret.put(row.id(), row);
      };
    return ret;
  };
  public Boolean updateFieldValues(
    MaritalStatusId id,
    List<MaritalStatusFieldValue<?>> fieldValues,
    Connection c
  ) {
    List<Fragment> updates = fieldValues.stream().map(
        fv -> switch (fv) {
                case id x -> interpolate(
                  typo.runtime.Fragment.lit("""
                  "id" = 
                  """),
                  MaritalStatusId.pgType.encode(x.value()),
                  typo.runtime.Fragment.lit("")
                );
              }
      ).toList();;
    return updates.isEmpty()
             ? false
             : interpolate(
                     typo.runtime.Fragment.lit("""
                     update "myschema"."marital_status"

                     """),
                     Fragment.set(updates),
                     typo.runtime.Fragment.lit("""
   
                        where "id" = """),
                     MaritalStatusId.pgType.encode(id),
                     typo.runtime.Fragment.lit("")
                   )
                 .update().runUnchecked(c) > 0;
  };
  public MaritalStatusRow upsert(
    MaritalStatusRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "myschema"."marital_status"("id")
         values ("""),
      MaritalStatusId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("""
         ::int8)
         on conflict ("id")
         do update set "id" = EXCLUDED."id"
         returning "id"
      """)
    )
      .updateReturning(MaritalStatusRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };
  public List<MaritalStatusRow> upsertBatch(
    Iterator<MaritalStatusRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "myschema"."marital_status"("id")
                values (?::int8)
                on conflict ("id")
                do nothing
                returning "id"
             """))
      .updateManyReturning(MaritalStatusRow._rowParser, unsaved)
      .runUnchecked(c);
  };
  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<MaritalStatusRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table marital_status_TEMP (like "myschema"."marital_status") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy marital_status_TEMP("id") from stdin
      """), batchSize, unsaved, c, MaritalStatusRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "myschema"."marital_status"("id")
              select * from marital_status_TEMP
              on conflict ("id")
              do nothing
              ;
              drop table marital_status_TEMP;""")).update().runUnchecked(c);
  };
}