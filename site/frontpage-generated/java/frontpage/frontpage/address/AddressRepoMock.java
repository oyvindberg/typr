/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.address;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record AddressRepoMock(
  java.util.function.Function<AddressRowUnsaved, AddressRow> toRow,
  HashMap<AddressId, AddressRow> map
) implements AddressRepo {
  public AddressRepoMock(java.util.function.Function<AddressRowUnsaved, AddressRow> toRow) {
    this(toRow, new HashMap<AddressId, AddressRow>());
  };

  public AddressRepoMock withToRow(java.util.function.Function<AddressRowUnsaved, AddressRow> toRow) {
    return new AddressRepoMock(toRow, map);
  };

  public AddressRepoMock withMap(HashMap<AddressId, AddressRow> map) {
    return new AddressRepoMock(toRow, map);
  };

  public DeleteBuilder<AddressFields, AddressRow> delete() {
    return new DeleteBuilderMock<>(AddressFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    AddressId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    AddressId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public AddressRow insert(
    AddressRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public AddressRow insert(
    AddressRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<AddressRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<AddressRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<AddressFields, AddressRow> select() {
    return new SelectBuilderMock<>(AddressFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<AddressRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<AddressRow> selectById(
    AddressId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<AddressRow> selectByIds(
    AddressId[] ids,
    Connection c
  ) {
    var result = new ArrayList<AddressRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<AddressId, AddressRow> selectByIdsTracked(
    AddressId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.address.AddressRow row) -> row.id(), Function.identity()));
  };

  public UpdateBuilder<AddressFields, AddressRow> update() {
    return new UpdateBuilderMock<>(AddressFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    AddressRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public AddressRow upsert(
    AddressRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<AddressRow> upsertBatch(
    Iterator<AddressRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<AddressRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<AddressRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}