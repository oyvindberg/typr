/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.category;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class CategoryRepoImpl implements CategoryRepo {
  public DeleteBuilder<CategoryFields, CategoryRow> delete() {
    return DeleteBuilder.of("frontpage.category", CategoryFields.structure());
  };

  public Boolean deleteById(
    CategoryId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."category" where "id" = 
      """),
      CategoryId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    CategoryId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."category"
                  where "id" = ANY("""),
               CategoryId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public CategoryRow insert(
    CategoryRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."category"("id", "name")
         values ("""),
      CategoryId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         returning "id", "name"
      """)
    )
      .updateReturning(CategoryRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public CategoryRow insert(
    CategoryRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            CategoryId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."category"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name"
        """)
      );;
    return q.updateReturning(CategoryRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<CategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."category"("id", "name") FROM STDIN
    """), batchSize, unsaved, c, CategoryRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<CategoryRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."category"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, CategoryRowUnsaved.pgText);
  };

  public SelectBuilder<CategoryFields, CategoryRow> select() {
    return SelectBuilder.of("frontpage.category", CategoryFields.structure(), CategoryRow._rowParser);
  };

  public List<CategoryRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name"
       from "frontpage"."category"
    """)).as(CategoryRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<CategoryRow> selectById(
    CategoryId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."category"
         where "id" = """),
      CategoryId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(CategoryRow._rowParser.first()).runUnchecked(c);
  };

  public List<CategoryRow> selectByIds(
    CategoryId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."category"
         where "id" = ANY("""),
      CategoryId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(CategoryRow._rowParser.all()).runUnchecked(c);
  };

  public Map<CategoryId, CategoryRow> selectByIdsTracked(
    CategoryId[] ids,
    Connection c
  ) {
    Map<CategoryId, CategoryRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public UpdateBuilder<CategoryFields, CategoryRow> update() {
    return UpdateBuilder.of("frontpage.category", CategoryFields.structure(), CategoryRow._rowParser.all());
  };

  public Boolean update(
    CategoryRow row,
    Connection c
  ) {
    CategoryId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."category"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
   
                where "id" = """),
             CategoryId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public CategoryRow upsert(
    CategoryRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."category"("id", "name")
         values ("""),
      CategoryId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name"
         returning "id", "name"
      """)
    )
      .updateReturning(CategoryRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<CategoryRow> upsertBatch(
    Iterator<CategoryRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."category"("id", "name")
                values (?::uuid, ?)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """))
      .updateManyReturning(CategoryRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<CategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table category_TEMP (like "frontpage"."category") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy category_TEMP("id", "name") from stdin
      """), batchSize, unsaved, c, CategoryRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."category"("id", "name")
              select * from category_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name"
              ;
              drop table category_TEMP;""")).update().runUnchecked(c);
  };
}