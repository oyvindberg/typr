/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user;

import frontpage.customtypes.TypoLocalDateTime;
import frontpage.frontpage.Email;
import frontpage.frontpage.UserRole;
import frontpage.frontpage.UserStatus;
import frontpage.frontpage.department.DepartmentId;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class UserRepoImpl implements UserRepo {
  public DeleteBuilder<UserFields, UserRow> delete() {
    return DeleteBuilder.of("frontpage.user", UserFields.structure());
  };

  public Boolean deleteById(
    UserId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."user" where "id" = 
      """),
      UserId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    UserId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."user"
                  where "id" = ANY("""),
               UserId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public UserRow insert(
    UserRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."user"("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role")
         values ("""),
      UserId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      Email.pgType.encode(unsaved.email()),
      typo.runtime.Fragment.lit("::text, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      TypoLocalDateTime.pgType.opt().encode(unsaved.createdAt()),
      typo.runtime.Fragment.lit("::timestamp, "),
      DepartmentId.pgType.opt().encode(unsaved.departmentId()),
      typo.runtime.Fragment.lit("::uuid, "),
      UserStatus.pgType.opt().encode(unsaved.status()),
      typo.runtime.Fragment.lit("::frontpage.user_status, "),
      PgTypes.bool.opt().encode(unsaved.verified()),
      typo.runtime.Fragment.lit(", "),
      UserId.pgType.opt().encode(unsaved.managerId()),
      typo.runtime.Fragment.lit("::uuid, "),
      UserRole.pgType.opt().encode(unsaved.role()),
      typo.runtime.Fragment.lit("""
         ::frontpage.user_role)
         returning "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
      """)
    )
      .updateReturning(UserRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public UserRow insert(
    UserRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"email\""));
      values.add(interpolate(
        Email.pgType.encode(unsaved.email()),
        typo.runtime.Fragment.lit("::text")
      ));
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      columns.add(Fragment.lit("\"department_id\""));
      values.add(interpolate(
        DepartmentId.pgType.opt().encode(unsaved.departmentId()),
        typo.runtime.Fragment.lit("::uuid")
      ));
      columns.add(Fragment.lit("\"manager_id\""));
      values.add(interpolate(
        UserId.pgType.opt().encode(unsaved.managerId()),
        typo.runtime.Fragment.lit("::uuid")
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            UserId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      unsaved.createdAt().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"created_at\""));
          values.add(interpolate(
            TypoLocalDateTime.pgType.opt().encode(value),
            typo.runtime.Fragment.lit("::timestamp")
          ));
        }
      );;
      unsaved.status().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"status\""));
          values.add(interpolate(
            UserStatus.pgType.opt().encode(value),
            typo.runtime.Fragment.lit("::frontpage.user_status")
          ));
        }
      );;
      unsaved.verified().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"verified\""));
          values.add(interpolate(
            PgTypes.bool.opt().encode(value),
            typo.runtime.Fragment.lit("""
            """)
          ));
        }
      );;
      unsaved.role().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"role\""));
          values.add(interpolate(
            UserRole.pgType.opt().encode(value),
            typo.runtime.Fragment.lit("::frontpage.user_role")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."user"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
        """)
      );;
    return q.updateReturning(UserRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<UserRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."user"("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role") FROM STDIN
    """), batchSize, unsaved, c, UserRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<UserRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."user"("email", "name", "department_id", "manager_id", "id", "created_at", "status", "verified", "role") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, UserRowUnsaved.pgText);
  };

  public SelectBuilder<UserFields, UserRow> select() {
    return SelectBuilder.of("frontpage.user", UserFields.structure(), UserRow._rowParser);
  };

  public List<UserRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
       from "frontpage"."user"
    """)).as(UserRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<UserRow> selectById(
    UserId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
         from "frontpage"."user"
         where "id" = """),
      UserId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(UserRow._rowParser.first()).runUnchecked(c);
  };

  public List<UserRow> selectByIds(
    UserId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
         from "frontpage"."user"
         where "id" = ANY("""),
      UserId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(UserRow._rowParser.all()).runUnchecked(c);
  };

  public Map<UserId, UserRow> selectByIdsTracked(
    UserId[] ids,
    Connection c
  ) {
    Map<UserId, UserRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public Optional<UserRow> selectByUniqueEmail(
    Email email,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
         from "frontpage"."user"
         where "email" = """),
      Email.pgType.encode(email),
      typo.runtime.Fragment.lit("""


      """)
    ).as(UserRow._rowParser.first()).runUnchecked(c);
  };

  public UpdateBuilder<UserFields, UserRow> update() {
    return UpdateBuilder.of("frontpage.user", UserFields.structure(), UserRow._rowParser.all());
  };

  public Boolean update(
    UserRow row,
    Connection c
  ) {
    UserId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."user"
                set "email" = """),
             Email.pgType.encode(row.email()),
             typo.runtime.Fragment.lit("""
                ::text,
                "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
                ,
                "created_at" = """),
             TypoLocalDateTime.pgType.opt().encode(row.createdAt()),
             typo.runtime.Fragment.lit("""
                ::timestamp,
                "department_id" = """),
             DepartmentId.pgType.opt().encode(row.departmentId()),
             typo.runtime.Fragment.lit("""
                ::uuid,
                "status" = """),
             UserStatus.pgType.opt().encode(row.status()),
             typo.runtime.Fragment.lit("""
                ::frontpage.user_status,
                "verified" = """),
             PgTypes.bool.opt().encode(row.verified()),
             typo.runtime.Fragment.lit("""
                ,
                "manager_id" = """),
             UserId.pgType.opt().encode(row.managerId()),
             typo.runtime.Fragment.lit("""
                ::uuid,
                "role" = """),
             UserRole.pgType.opt().encode(row.role()),
             typo.runtime.Fragment.lit("""
                ::frontpage.user_role
                where "id" = """),
             UserId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public UserRow upsert(
    UserRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."user"("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role")
         values ("""),
      UserId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      Email.pgType.encode(unsaved.email()),
      typo.runtime.Fragment.lit("::text, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      TypoLocalDateTime.pgType.opt().encode(unsaved.createdAt()),
      typo.runtime.Fragment.lit("::timestamp, "),
      DepartmentId.pgType.opt().encode(unsaved.departmentId()),
      typo.runtime.Fragment.lit("::uuid, "),
      UserStatus.pgType.opt().encode(unsaved.status()),
      typo.runtime.Fragment.lit("::frontpage.user_status, "),
      PgTypes.bool.opt().encode(unsaved.verified()),
      typo.runtime.Fragment.lit(", "),
      UserId.pgType.opt().encode(unsaved.managerId()),
      typo.runtime.Fragment.lit("::uuid, "),
      UserRole.pgType.opt().encode(unsaved.role()),
      typo.runtime.Fragment.lit("""
         ::frontpage.user_role)
         on conflict ("id")
         do update set
           "email" = EXCLUDED."email",
         "name" = EXCLUDED."name",
         "created_at" = EXCLUDED."created_at",
         "department_id" = EXCLUDED."department_id",
         "status" = EXCLUDED."status",
         "verified" = EXCLUDED."verified",
         "manager_id" = EXCLUDED."manager_id",
         "role" = EXCLUDED."role"
         returning "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
      """)
    )
      .updateReturning(UserRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<UserRow> upsertBatch(
    Iterator<UserRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."user"("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role")
                values (?::uuid, ?::text, ?, ?::timestamp, ?::uuid, ?::frontpage.user_status, ?, ?::uuid, ?::frontpage.user_role)
                on conflict ("id")
                do update set
                  "email" = EXCLUDED."email",
                "name" = EXCLUDED."name",
                "created_at" = EXCLUDED."created_at",
                "department_id" = EXCLUDED."department_id",
                "status" = EXCLUDED."status",
                "verified" = EXCLUDED."verified",
                "manager_id" = EXCLUDED."manager_id",
                "role" = EXCLUDED."role"
                returning "id", "email", "name", "created_at"::text, "department_id", "status", "verified", "manager_id", "role"
             """))
      .updateManyReturning(UserRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<UserRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table user_TEMP (like "frontpage"."user") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy user_TEMP("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role") from stdin
      """), batchSize, unsaved, c, UserRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."user"("id", "email", "name", "created_at", "department_id", "status", "verified", "manager_id", "role")
              select * from user_TEMP
              on conflict ("id")
              do update set
                "email" = EXCLUDED."email",
              "name" = EXCLUDED."name",
              "created_at" = EXCLUDED."created_at",
              "department_id" = EXCLUDED."department_id",
              "status" = EXCLUDED."status",
              "verified" = EXCLUDED."verified",
              "manager_id" = EXCLUDED."manager_id",
              "role" = EXCLUDED."role"
              ;
              drop table user_TEMP;""")).update().runUnchecked(c);
  };
}