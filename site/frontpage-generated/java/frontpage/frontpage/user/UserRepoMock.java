/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user;

import frontpage.frontpage.Email;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record UserRepoMock(
  java.util.function.Function<UserRowUnsaved, UserRow> toRow,
  HashMap<UserId, UserRow> map
) implements UserRepo {
  public UserRepoMock(java.util.function.Function<UserRowUnsaved, UserRow> toRow) {
    this(toRow, new HashMap<UserId, UserRow>());
  };

  public UserRepoMock withToRow(java.util.function.Function<UserRowUnsaved, UserRow> toRow) {
    return new UserRepoMock(toRow, map);
  };

  public UserRepoMock withMap(HashMap<UserId, UserRow> map) {
    return new UserRepoMock(toRow, map);
  };

  public DeleteBuilder<UserFields, UserRow> delete() {
    return new DeleteBuilderMock<>(UserFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    UserId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    UserId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public UserRow insert(
    UserRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public UserRow insert(
    UserRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<UserRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<UserRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<UserFields, UserRow> select() {
    return new SelectBuilderMock<>(UserFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<UserRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<UserRow> selectById(
    UserId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<UserRow> selectByIds(
    UserId[] ids,
    Connection c
  ) {
    var result = new ArrayList<UserRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<UserId, UserRow> selectByIdsTracked(
    UserId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.user.UserRow row) -> row.id(), Function.identity()));
  };

  public Optional<UserRow> selectByUniqueEmail(
    Email email,
    Connection c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> email.equals(v.email())).findFirst();
  };

  public UpdateBuilder<UserFields, UserRow> update() {
    return new UpdateBuilderMock<>(UserFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    UserRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public UserRow upsert(
    UserRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<UserRow> upsertBatch(
    Iterator<UserRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<UserRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<UserRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}