/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.person;

import frontpage.customtypes.TypoLocalDateTime;
import frontpage.frontpage.address.AddressId;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class PersonRepoImpl implements PersonRepo {
  public DeleteBuilder<PersonFields, PersonRow> delete() {
    return DeleteBuilder.of("frontpage.person", PersonFields.structure());
  };

  public Boolean deleteById(
    PersonId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."person" where "id" = 
      """),
      PersonId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    PersonId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."person"
                  where "id" = ANY("""),
               PersonId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public PersonRow insert(
    PersonRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."person"("id", "name", "address_id", "created_at")
         values ("""),
      PersonId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      AddressId.pgType.opt().encode(unsaved.addressId()),
      typo.runtime.Fragment.lit("::uuid, "),
      TypoLocalDateTime.pgType.opt().encode(unsaved.createdAt()),
      typo.runtime.Fragment.lit("""
         ::timestamp)
         returning "id", "name", "address_id", "created_at"::text
      """)
    )
      .updateReturning(PersonRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public PersonRow insert(
    PersonRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      columns.add(Fragment.lit("\"address_id\""));
      values.add(interpolate(
        AddressId.pgType.opt().encode(unsaved.addressId()),
        typo.runtime.Fragment.lit("::uuid")
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            PersonId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      unsaved.createdAt().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"created_at\""));
          values.add(interpolate(
            TypoLocalDateTime.pgType.opt().encode(value),
            typo.runtime.Fragment.lit("::timestamp")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."person"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name", "address_id", "created_at"::text
        """)
      );;
    return q.updateReturning(PersonRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<PersonRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."person"("id", "name", "address_id", "created_at") FROM STDIN
    """), batchSize, unsaved, c, PersonRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<PersonRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."person"("name", "address_id", "id", "created_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, PersonRowUnsaved.pgText);
  };

  public SelectBuilder<PersonFields, PersonRow> select() {
    return SelectBuilder.of("frontpage.person", PersonFields.structure(), PersonRow._rowParser);
  };

  public List<PersonRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name", "address_id", "created_at"::text
       from "frontpage"."person"
    """)).as(PersonRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<PersonRow> selectById(
    PersonId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "address_id", "created_at"::text
         from "frontpage"."person"
         where "id" = """),
      PersonId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(PersonRow._rowParser.first()).runUnchecked(c);
  };

  public List<PersonRow> selectByIds(
    PersonId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "address_id", "created_at"::text
         from "frontpage"."person"
         where "id" = ANY("""),
      PersonId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(PersonRow._rowParser.all()).runUnchecked(c);
  };

  public Map<PersonId, PersonRow> selectByIdsTracked(
    PersonId[] ids,
    Connection c
  ) {
    Map<PersonId, PersonRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public UpdateBuilder<PersonFields, PersonRow> update() {
    return UpdateBuilder.of("frontpage.person", PersonFields.structure(), PersonRow._rowParser.all());
  };

  public Boolean update(
    PersonRow row,
    Connection c
  ) {
    PersonId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."person"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
                ,
                "address_id" = """),
             AddressId.pgType.opt().encode(row.addressId()),
             typo.runtime.Fragment.lit("""
                ::uuid,
                "created_at" = """),
             TypoLocalDateTime.pgType.opt().encode(row.createdAt()),
             typo.runtime.Fragment.lit("""
                ::timestamp
                where "id" = """),
             PersonId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public PersonRow upsert(
    PersonRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."person"("id", "name", "address_id", "created_at")
         values ("""),
      PersonId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      AddressId.pgType.opt().encode(unsaved.addressId()),
      typo.runtime.Fragment.lit("::uuid, "),
      TypoLocalDateTime.pgType.opt().encode(unsaved.createdAt()),
      typo.runtime.Fragment.lit("""
         ::timestamp)
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name",
         "address_id" = EXCLUDED."address_id",
         "created_at" = EXCLUDED."created_at"
         returning "id", "name", "address_id", "created_at"::text
      """)
    )
      .updateReturning(PersonRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<PersonRow> upsertBatch(
    Iterator<PersonRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."person"("id", "name", "address_id", "created_at")
                values (?::uuid, ?, ?::uuid, ?::timestamp)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                "address_id" = EXCLUDED."address_id",
                "created_at" = EXCLUDED."created_at"
                returning "id", "name", "address_id", "created_at"::text
             """))
      .updateManyReturning(PersonRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<PersonRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table person_TEMP (like "frontpage"."person") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy person_TEMP("id", "name", "address_id", "created_at") from stdin
      """), batchSize, unsaved, c, PersonRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."person"("id", "name", "address_id", "created_at")
              select * from person_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name",
              "address_id" = EXCLUDED."address_id",
              "created_at" = EXCLUDED."created_at"
              ;
              drop table person_TEMP;""")).update().runUnchecked(c);
  };
}