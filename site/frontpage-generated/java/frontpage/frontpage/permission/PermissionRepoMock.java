/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record PermissionRepoMock(
  java.util.function.Function<PermissionRowUnsaved, PermissionRow> toRow,
  HashMap<PermissionId, PermissionRow> map
) implements PermissionRepo {
  public PermissionRepoMock(java.util.function.Function<PermissionRowUnsaved, PermissionRow> toRow) {
    this(toRow, new HashMap<PermissionId, PermissionRow>());
  };

  public PermissionRepoMock withToRow(java.util.function.Function<PermissionRowUnsaved, PermissionRow> toRow) {
    return new PermissionRepoMock(toRow, map);
  };

  public PermissionRepoMock withMap(HashMap<PermissionId, PermissionRow> map) {
    return new PermissionRepoMock(toRow, map);
  };

  public DeleteBuilder<PermissionFields, PermissionRow> delete() {
    return new DeleteBuilderMock<>(PermissionFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    PermissionId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    PermissionId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public PermissionRow insert(
    PermissionRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public PermissionRow insert(
    PermissionRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<PermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<PermissionRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<PermissionFields, PermissionRow> select() {
    return new SelectBuilderMock<>(PermissionFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<PermissionRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<PermissionRow> selectById(
    PermissionId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<PermissionRow> selectByIds(
    PermissionId[] ids,
    Connection c
  ) {
    var result = new ArrayList<PermissionRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<PermissionId, PermissionRow> selectByIdsTracked(
    PermissionId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.permission.PermissionRow row) -> row.id(), Function.identity()));
  };

  public Optional<PermissionRow> selectByUniqueName(
    String name,
    Connection c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> name.equals(v.name())).findFirst();
  };

  public UpdateBuilder<PermissionFields, PermissionRow> update() {
    return new UpdateBuilderMock<>(PermissionFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    PermissionRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public PermissionRow upsert(
    PermissionRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<PermissionRow> upsertBatch(
    Iterator<PermissionRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<PermissionRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<PermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}