/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class PermissionRepoImpl implements PermissionRepo {
  public DeleteBuilder<PermissionFields, PermissionRow> delete() {
    return DeleteBuilder.of("frontpage.permission", PermissionFields.structure());
  };

  public Boolean deleteById(
    PermissionId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."permission" where "id" = 
      """),
      PermissionId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    PermissionId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."permission"
                  where "id" = ANY("""),
               PermissionId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public PermissionRow insert(
    PermissionRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."permission"("id", "name")
         values ("""),
      PermissionId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         returning "id", "name"
      """)
    )
      .updateReturning(PermissionRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public PermissionRow insert(
    PermissionRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            PermissionId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."permission"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name"
        """)
      );;
    return q.updateReturning(PermissionRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<PermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."permission"("id", "name") FROM STDIN
    """), batchSize, unsaved, c, PermissionRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<PermissionRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."permission"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, PermissionRowUnsaved.pgText);
  };

  public SelectBuilder<PermissionFields, PermissionRow> select() {
    return SelectBuilder.of("frontpage.permission", PermissionFields.structure(), PermissionRow._rowParser);
  };

  public List<PermissionRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name"
       from "frontpage"."permission"
    """)).as(PermissionRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<PermissionRow> selectById(
    PermissionId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."permission"
         where "id" = """),
      PermissionId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(PermissionRow._rowParser.first()).runUnchecked(c);
  };

  public List<PermissionRow> selectByIds(
    PermissionId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."permission"
         where "id" = ANY("""),
      PermissionId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(PermissionRow._rowParser.all()).runUnchecked(c);
  };

  public Map<PermissionId, PermissionRow> selectByIdsTracked(
    PermissionId[] ids,
    Connection c
  ) {
    Map<PermissionId, PermissionRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public Optional<PermissionRow> selectByUniqueName(
    String name,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."permission"
         where "name" = """),
      PgTypes.text.encode(name),
      typo.runtime.Fragment.lit("""


      """)
    ).as(PermissionRow._rowParser.first()).runUnchecked(c);
  };

  public UpdateBuilder<PermissionFields, PermissionRow> update() {
    return UpdateBuilder.of("frontpage.permission", PermissionFields.structure(), PermissionRow._rowParser.all());
  };

  public Boolean update(
    PermissionRow row,
    Connection c
  ) {
    PermissionId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."permission"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
   
                where "id" = """),
             PermissionId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public PermissionRow upsert(
    PermissionRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."permission"("id", "name")
         values ("""),
      PermissionId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name"
         returning "id", "name"
      """)
    )
      .updateReturning(PermissionRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<PermissionRow> upsertBatch(
    Iterator<PermissionRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."permission"("id", "name")
                values (?::uuid, ?)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """))
      .updateManyReturning(PermissionRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<PermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table permission_TEMP (like "frontpage"."permission") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy permission_TEMP("id", "name") from stdin
      """), batchSize, unsaved, c, PermissionRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."permission"("id", "name")
              select * from permission_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name"
              ;
              drop table permission_TEMP;""")).update().runUnchecked(c);
  };
}