/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.product_category;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record ProductCategoryRepoMock(HashMap<ProductCategoryId, ProductCategoryRow> map) implements ProductCategoryRepo {
  public ProductCategoryRepoMock() {
    this(new HashMap<ProductCategoryId, ProductCategoryRow>());
  };

  public ProductCategoryRepoMock withMap(HashMap<ProductCategoryId, ProductCategoryRow> map) {
    return new ProductCategoryRepoMock(map);
  };

  public DeleteBuilder<ProductCategoryFields, ProductCategoryRow> delete() {
    return new DeleteBuilderMock<>(ProductCategoryFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  public Boolean deleteById(
    ProductCategoryId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  public Integer deleteByIds(
    ProductCategoryId[] compositeIds,
    Connection c
  ) {
    var count = 0;
      for (var id : compositeIds) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public ProductCategoryRow insert(
    ProductCategoryRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
        throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
      };
      map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public Long insertStreaming(
    Iterator<ProductCategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<ProductCategoryFields, ProductCategoryRow> select() {
    return new SelectBuilderMock<>(ProductCategoryFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<ProductCategoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<ProductCategoryRow> selectById(
    ProductCategoryId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  public List<ProductCategoryRow> selectByIds(
    ProductCategoryId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<ProductCategoryRow>();
      for (var id : compositeIds) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<ProductCategoryId, ProductCategoryRow> selectByIdsTracked(
    ProductCategoryId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((frontpage.frontpage.product_category.ProductCategoryRow row) -> row.compositeId(), Function.identity()));
  };

  public UpdateBuilder<ProductCategoryFields, ProductCategoryRow> update() {
    return new UpdateBuilderMock<>(ProductCategoryFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public ProductCategoryRow upsert(
    ProductCategoryRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public List<ProductCategoryRow> upsertBatch(
    Iterator<ProductCategoryRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ProductCategoryRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<ProductCategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1;
      };
    return count;
  };
}