/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user_permission;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record UserPermissionRepoMock(
  java.util.function.Function<UserPermissionRowUnsaved, UserPermissionRow> toRow,
  HashMap<UserPermissionId, UserPermissionRow> map
) implements UserPermissionRepo {
  public UserPermissionRepoMock(java.util.function.Function<UserPermissionRowUnsaved, UserPermissionRow> toRow) {
    this(toRow, new HashMap<UserPermissionId, UserPermissionRow>());
  };

  public UserPermissionRepoMock withToRow(java.util.function.Function<UserPermissionRowUnsaved, UserPermissionRow> toRow) {
    return new UserPermissionRepoMock(toRow, map);
  };

  public UserPermissionRepoMock withMap(HashMap<UserPermissionId, UserPermissionRow> map) {
    return new UserPermissionRepoMock(toRow, map);
  };

  public DeleteBuilder<UserPermissionFields, UserPermissionRow> delete() {
    return new DeleteBuilderMock<>(UserPermissionFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  public Boolean deleteById(
    UserPermissionId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  public Integer deleteByIds(
    UserPermissionId[] compositeIds,
    Connection c
  ) {
    var count = 0;
      for (var id : compositeIds) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public UserPermissionRow insert(
    UserPermissionRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
        throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
      };
      map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public UserPermissionRow insert(
    UserPermissionRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<UserPermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<UserPermissionRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.compositeId(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<UserPermissionFields, UserPermissionRow> select() {
    return new SelectBuilderMock<>(UserPermissionFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<UserPermissionRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<UserPermissionRow> selectById(
    UserPermissionId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  public List<UserPermissionRow> selectByIds(
    UserPermissionId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<UserPermissionRow>();
      for (var id : compositeIds) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<UserPermissionId, UserPermissionRow> selectByIdsTracked(
    UserPermissionId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((frontpage.frontpage.user_permission.UserPermissionRow row) -> row.compositeId(), Function.identity()));
  };

  public UpdateBuilder<UserPermissionFields, UserPermissionRow> update() {
    return new UpdateBuilderMock<>(UserPermissionFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    UserPermissionRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.compositeId(), row);
      };
    return shouldUpdate;
  };

  public UserPermissionRow upsert(
    UserPermissionRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public List<UserPermissionRow> upsertBatch(
    Iterator<UserPermissionRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<UserPermissionRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<UserPermissionRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1;
      };
    return count;
  };
}