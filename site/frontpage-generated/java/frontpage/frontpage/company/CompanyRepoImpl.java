/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.company;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class CompanyRepoImpl implements CompanyRepo {
  public DeleteBuilder<CompanyFields, CompanyRow> delete() {
    return DeleteBuilder.of("frontpage.company", CompanyFields.structure());
  };

  public Boolean deleteById(
    CompanyId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."company" where "id" = 
      """),
      CompanyId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    CompanyId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."company"
                  where "id" = ANY("""),
               CompanyId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public CompanyRow insert(
    CompanyRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."company"("id", "name")
         values ("""),
      CompanyId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         returning "id", "name"
      """)
    )
      .updateReturning(CompanyRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public CompanyRow insert(
    CompanyRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            CompanyId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."company"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name"
        """)
      );;
    return q.updateReturning(CompanyRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<CompanyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."company"("id", "name") FROM STDIN
    """), batchSize, unsaved, c, CompanyRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<CompanyRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."company"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, CompanyRowUnsaved.pgText);
  };

  public SelectBuilder<CompanyFields, CompanyRow> select() {
    return SelectBuilder.of("frontpage.company", CompanyFields.structure(), CompanyRow._rowParser);
  };

  public List<CompanyRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name"
       from "frontpage"."company"
    """)).as(CompanyRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<CompanyRow> selectById(
    CompanyId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."company"
         where "id" = """),
      CompanyId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(CompanyRow._rowParser.first()).runUnchecked(c);
  };

  public List<CompanyRow> selectByIds(
    CompanyId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name"
         from "frontpage"."company"
         where "id" = ANY("""),
      CompanyId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(CompanyRow._rowParser.all()).runUnchecked(c);
  };

  public Map<CompanyId, CompanyRow> selectByIdsTracked(
    CompanyId[] ids,
    Connection c
  ) {
    Map<CompanyId, CompanyRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public UpdateBuilder<CompanyFields, CompanyRow> update() {
    return UpdateBuilder.of("frontpage.company", CompanyFields.structure(), CompanyRow._rowParser.all());
  };

  public Boolean update(
    CompanyRow row,
    Connection c
  ) {
    CompanyId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."company"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
   
                where "id" = """),
             CompanyId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public CompanyRow upsert(
    CompanyRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."company"("id", "name")
         values ("""),
      CompanyId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit("""
         )
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name"
         returning "id", "name"
      """)
    )
      .updateReturning(CompanyRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<CompanyRow> upsertBatch(
    Iterator<CompanyRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."company"("id", "name")
                values (?::uuid, ?)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """))
      .updateManyReturning(CompanyRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<CompanyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table company_TEMP (like "frontpage"."company") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy company_TEMP("id", "name") from stdin
      """), batchSize, unsaved, c, CompanyRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."company"("id", "name")
              select * from company_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name"
              ;
              drop table company_TEMP;""")).update().runUnchecked(c);
  };
}