/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.company;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record CompanyRepoMock(
  java.util.function.Function<CompanyRowUnsaved, CompanyRow> toRow,
  HashMap<CompanyId, CompanyRow> map
) implements CompanyRepo {
  public CompanyRepoMock(java.util.function.Function<CompanyRowUnsaved, CompanyRow> toRow) {
    this(toRow, new HashMap<CompanyId, CompanyRow>());
  };

  public CompanyRepoMock withToRow(java.util.function.Function<CompanyRowUnsaved, CompanyRow> toRow) {
    return new CompanyRepoMock(toRow, map);
  };

  public CompanyRepoMock withMap(HashMap<CompanyId, CompanyRow> map) {
    return new CompanyRepoMock(toRow, map);
  };

  public DeleteBuilder<CompanyFields, CompanyRow> delete() {
    return new DeleteBuilderMock<>(CompanyFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    CompanyId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    CompanyId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public CompanyRow insert(
    CompanyRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public CompanyRow insert(
    CompanyRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<CompanyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<CompanyRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<CompanyFields, CompanyRow> select() {
    return new SelectBuilderMock<>(CompanyFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<CompanyRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<CompanyRow> selectById(
    CompanyId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<CompanyRow> selectByIds(
    CompanyId[] ids,
    Connection c
  ) {
    var result = new ArrayList<CompanyRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<CompanyId, CompanyRow> selectByIdsTracked(
    CompanyId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.company.CompanyRow row) -> row.id(), Function.identity()));
  };

  public UpdateBuilder<CompanyFields, CompanyRow> update() {
    return new UpdateBuilderMock<>(CompanyFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    CompanyRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public CompanyRow upsert(
    CompanyRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<CompanyRow> upsertBatch(
    Iterator<CompanyRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<CompanyRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<CompanyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}