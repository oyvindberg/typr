/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.department;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record DepartmentRepoMock(
  java.util.function.Function<DepartmentRowUnsaved, DepartmentRow> toRow,
  HashMap<DepartmentId, DepartmentRow> map
) implements DepartmentRepo {
  public DepartmentRepoMock(java.util.function.Function<DepartmentRowUnsaved, DepartmentRow> toRow) {
    this(toRow, new HashMap<DepartmentId, DepartmentRow>());
  };

  public DepartmentRepoMock withToRow(java.util.function.Function<DepartmentRowUnsaved, DepartmentRow> toRow) {
    return new DepartmentRepoMock(toRow, map);
  };

  public DepartmentRepoMock withMap(HashMap<DepartmentId, DepartmentRow> map) {
    return new DepartmentRepoMock(toRow, map);
  };

  public DeleteBuilder<DepartmentFields, DepartmentRow> delete() {
    return new DeleteBuilderMock<>(DepartmentFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    DepartmentId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    DepartmentId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public DepartmentRow insert(
    DepartmentRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public DepartmentRow insert(
    DepartmentRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<DepartmentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<DepartmentRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<DepartmentFields, DepartmentRow> select() {
    return new SelectBuilderMock<>(DepartmentFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<DepartmentRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<DepartmentRow> selectById(
    DepartmentId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<DepartmentRow> selectByIds(
    DepartmentId[] ids,
    Connection c
  ) {
    var result = new ArrayList<DepartmentRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<DepartmentId, DepartmentRow> selectByIdsTracked(
    DepartmentId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.department.DepartmentRow row) -> row.id(), Function.identity()));
  };

  public UpdateBuilder<DepartmentFields, DepartmentRow> update() {
    return new UpdateBuilderMock<>(DepartmentFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    DepartmentRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public DepartmentRow upsert(
    DepartmentRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<DepartmentRow> upsertBatch(
    Iterator<DepartmentRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<DepartmentRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<DepartmentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}