/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.department;

import frontpage.frontpage.company.CompanyId;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class DepartmentRepoImpl implements DepartmentRepo {
  public DeleteBuilder<DepartmentFields, DepartmentRow> delete() {
    return DeleteBuilder.of("frontpage.department", DepartmentFields.structure());
  };

  public Boolean deleteById(
    DepartmentId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."department" where "id" = 
      """),
      DepartmentId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    DepartmentId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."department"
                  where "id" = ANY("""),
               DepartmentId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public DepartmentRow insert(
    DepartmentRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."department"("id", "name", "budget", "company_id")
         values ("""),
      DepartmentId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      PgTypes.numeric.opt().encode(unsaved.budget()),
      typo.runtime.Fragment.lit("::numeric, "),
      CompanyId.pgType.opt().encode(unsaved.companyId()),
      typo.runtime.Fragment.lit("""
         ::uuid)
         returning "id", "name", "budget", "company_id"
      """)
    )
      .updateReturning(DepartmentRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public DepartmentRow insert(
    DepartmentRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      columns.add(Fragment.lit("\"budget\""));
      values.add(interpolate(
        PgTypes.numeric.opt().encode(unsaved.budget()),
        typo.runtime.Fragment.lit("::numeric")
      ));
      columns.add(Fragment.lit("\"company_id\""));
      values.add(interpolate(
        CompanyId.pgType.opt().encode(unsaved.companyId()),
        typo.runtime.Fragment.lit("::uuid")
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            DepartmentId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."department"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name", "budget", "company_id"
        """)
      );;
    return q.updateReturning(DepartmentRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<DepartmentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."department"("id", "name", "budget", "company_id") FROM STDIN
    """), batchSize, unsaved, c, DepartmentRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<DepartmentRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."department"("name", "budget", "company_id", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, DepartmentRowUnsaved.pgText);
  };

  public SelectBuilder<DepartmentFields, DepartmentRow> select() {
    return SelectBuilder.of("frontpage.department", DepartmentFields.structure(), DepartmentRow._rowParser);
  };

  public List<DepartmentRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name", "budget", "company_id"
       from "frontpage"."department"
    """)).as(DepartmentRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<DepartmentRow> selectById(
    DepartmentId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "budget", "company_id"
         from "frontpage"."department"
         where "id" = """),
      DepartmentId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(DepartmentRow._rowParser.first()).runUnchecked(c);
  };

  public List<DepartmentRow> selectByIds(
    DepartmentId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "budget", "company_id"
         from "frontpage"."department"
         where "id" = ANY("""),
      DepartmentId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(DepartmentRow._rowParser.all()).runUnchecked(c);
  };

  public Map<DepartmentId, DepartmentRow> selectByIdsTracked(
    DepartmentId[] ids,
    Connection c
  ) {
    Map<DepartmentId, DepartmentRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public UpdateBuilder<DepartmentFields, DepartmentRow> update() {
    return UpdateBuilder.of("frontpage.department", DepartmentFields.structure(), DepartmentRow._rowParser.all());
  };

  public Boolean update(
    DepartmentRow row,
    Connection c
  ) {
    DepartmentId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."department"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
                ,
                "budget" = """),
             PgTypes.numeric.opt().encode(row.budget()),
             typo.runtime.Fragment.lit("""
                ::numeric,
                "company_id" = """),
             CompanyId.pgType.opt().encode(row.companyId()),
             typo.runtime.Fragment.lit("""
                ::uuid
                where "id" = """),
             DepartmentId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public DepartmentRow upsert(
    DepartmentRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."department"("id", "name", "budget", "company_id")
         values ("""),
      DepartmentId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      PgTypes.numeric.opt().encode(unsaved.budget()),
      typo.runtime.Fragment.lit("::numeric, "),
      CompanyId.pgType.opt().encode(unsaved.companyId()),
      typo.runtime.Fragment.lit("""
         ::uuid)
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name",
         "budget" = EXCLUDED."budget",
         "company_id" = EXCLUDED."company_id"
         returning "id", "name", "budget", "company_id"
      """)
    )
      .updateReturning(DepartmentRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<DepartmentRow> upsertBatch(
    Iterator<DepartmentRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."department"("id", "name", "budget", "company_id")
                values (?::uuid, ?, ?::numeric, ?::uuid)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                "budget" = EXCLUDED."budget",
                "company_id" = EXCLUDED."company_id"
                returning "id", "name", "budget", "company_id"
             """))
      .updateManyReturning(DepartmentRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<DepartmentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table department_TEMP (like "frontpage"."department") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy department_TEMP("id", "name", "budget", "company_id") from stdin
      """), batchSize, unsaved, c, DepartmentRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."department"("id", "name", "budget", "company_id")
              select * from department_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name",
              "budget" = EXCLUDED."budget",
              "company_id" = EXCLUDED."company_id"
              ;
              drop table department_TEMP;""")).update().runUnchecked(c);
  };
}