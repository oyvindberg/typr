/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.role;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record RoleRepoMock(
  java.util.function.Function<RoleRowUnsaved, RoleRow> toRow,
  HashMap<RoleId, RoleRow> map
) implements RoleRepo {
  public RoleRepoMock(java.util.function.Function<RoleRowUnsaved, RoleRow> toRow) {
    this(toRow, new HashMap<RoleId, RoleRow>());
  };

  public RoleRepoMock withToRow(java.util.function.Function<RoleRowUnsaved, RoleRow> toRow) {
    return new RoleRepoMock(toRow, map);
  };

  public RoleRepoMock withMap(HashMap<RoleId, RoleRow> map) {
    return new RoleRepoMock(toRow, map);
  };

  public DeleteBuilder<RoleFields, RoleRow> delete() {
    return new DeleteBuilderMock<>(RoleFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    RoleId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    RoleId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public RoleRow insert(
    RoleRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public RoleRow insert(
    RoleRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<RoleRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<RoleRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<RoleFields, RoleRow> select() {
    return new SelectBuilderMock<>(RoleFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<RoleRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<RoleRow> selectById(
    RoleId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<RoleRow> selectByIds(
    RoleId[] ids,
    Connection c
  ) {
    var result = new ArrayList<RoleRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<RoleId, RoleRow> selectByIdsTracked(
    RoleId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.role.RoleRow row) -> row.id(), Function.identity()));
  };

  public Optional<RoleRow> selectByUniqueName(
    String name,
    Connection c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> name.equals(v.name())).findFirst();
  };

  public UpdateBuilder<RoleFields, RoleRow> update() {
    return new UpdateBuilderMock<>(RoleFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    RoleRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public RoleRow upsert(
    RoleRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<RoleRow> upsertBatch(
    Iterator<RoleRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<RoleRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<RoleRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}