/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.location;

import frontpage.customtypes.TypoInet;
import frontpage.customtypes.TypoJsonb;
import frontpage.customtypes.TypoPoint;
import frontpage.customtypes.TypoPolygon;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class LocationRepoImpl implements LocationRepo {
  public DeleteBuilder<LocationFields, LocationRow> delete() {
    return DeleteBuilder.of("frontpage.location", LocationFields.structure());
  };

  public Boolean deleteById(
    LocationId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."location" where "id" = 
      """),
      LocationId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    LocationId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."location"
                  where "id" = ANY("""),
               LocationId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public LocationRow insert(
    LocationRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
         values ("""),
      LocationId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      TypoPoint.pgType.opt().encode(unsaved.position()),
      typo.runtime.Fragment.lit("::point, "),
      TypoPolygon.pgType.opt().encode(unsaved.area()),
      typo.runtime.Fragment.lit("::polygon, "),
      TypoInet.pgType.opt().encode(unsaved.ipRange()),
      typo.runtime.Fragment.lit("::inet, "),
      TypoJsonb.pgType.opt().encode(unsaved.metadata()),
      typo.runtime.Fragment.lit("""
         ::jsonb)
         returning "id", "name", "position", "area", "ip_range", "metadata"
      """)
    )
      .updateReturning(LocationRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public LocationRow insert(
    LocationRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"name\""));
      values.add(interpolate(
        PgTypes.text.encode(unsaved.name()),
        typo.runtime.Fragment.lit("""
        """)
      ));
      columns.add(Fragment.lit("\"position\""));
      values.add(interpolate(
        TypoPoint.pgType.opt().encode(unsaved.position()),
        typo.runtime.Fragment.lit("::point")
      ));
      columns.add(Fragment.lit("\"area\""));
      values.add(interpolate(
        TypoPolygon.pgType.opt().encode(unsaved.area()),
        typo.runtime.Fragment.lit("::polygon")
      ));
      columns.add(Fragment.lit("\"ip_range\""));
      values.add(interpolate(
        TypoInet.pgType.opt().encode(unsaved.ipRange()),
        typo.runtime.Fragment.lit("::inet")
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            LocationId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      unsaved.metadata().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"metadata\""));
          values.add(interpolate(
            TypoJsonb.pgType.opt().encode(value),
            typo.runtime.Fragment.lit("::jsonb")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."location"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "name", "position", "area", "ip_range", "metadata"
        """)
      );;
    return q.updateReturning(LocationRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<LocationRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata") FROM STDIN
    """), batchSize, unsaved, c, LocationRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<LocationRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."location"("name", "position", "area", "ip_range", "id", "metadata") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, LocationRowUnsaved.pgText);
  };

  public SelectBuilder<LocationFields, LocationRow> select() {
    return SelectBuilder.of("frontpage.location", LocationFields.structure(), LocationRow._rowParser);
  };

  public List<LocationRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "name", "position", "area", "ip_range", "metadata"
       from "frontpage"."location"
    """)).as(LocationRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<LocationRow> selectById(
    LocationId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "position", "area", "ip_range", "metadata"
         from "frontpage"."location"
         where "id" = """),
      LocationId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(LocationRow._rowParser.first()).runUnchecked(c);
  };

  public List<LocationRow> selectByIds(
    LocationId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "name", "position", "area", "ip_range", "metadata"
         from "frontpage"."location"
         where "id" = ANY("""),
      LocationId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(LocationRow._rowParser.all()).runUnchecked(c);
  };

  public Map<LocationId, LocationRow> selectByIdsTracked(
    LocationId[] ids,
    Connection c
  ) {
    Map<LocationId, LocationRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public UpdateBuilder<LocationFields, LocationRow> update() {
    return UpdateBuilder.of("frontpage.location", LocationFields.structure(), LocationRow._rowParser.all());
  };

  public Boolean update(
    LocationRow row,
    Connection c
  ) {
    LocationId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."location"
                set "name" = """),
             PgTypes.text.encode(row.name()),
             typo.runtime.Fragment.lit("""
                ,
                "position" = """),
             TypoPoint.pgType.opt().encode(row.position()),
             typo.runtime.Fragment.lit("""
                ::point,
                "area" = """),
             TypoPolygon.pgType.opt().encode(row.area()),
             typo.runtime.Fragment.lit("""
                ::polygon,
                "ip_range" = """),
             TypoInet.pgType.opt().encode(row.ipRange()),
             typo.runtime.Fragment.lit("""
                ::inet,
                "metadata" = """),
             TypoJsonb.pgType.opt().encode(row.metadata()),
             typo.runtime.Fragment.lit("""
                ::jsonb
                where "id" = """),
             LocationId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public LocationRow upsert(
    LocationRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
         values ("""),
      LocationId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.text.encode(unsaved.name()),
      typo.runtime.Fragment.lit(", "),
      TypoPoint.pgType.opt().encode(unsaved.position()),
      typo.runtime.Fragment.lit("::point, "),
      TypoPolygon.pgType.opt().encode(unsaved.area()),
      typo.runtime.Fragment.lit("::polygon, "),
      TypoInet.pgType.opt().encode(unsaved.ipRange()),
      typo.runtime.Fragment.lit("::inet, "),
      TypoJsonb.pgType.opt().encode(unsaved.metadata()),
      typo.runtime.Fragment.lit("""
         ::jsonb)
         on conflict ("id")
         do update set
           "name" = EXCLUDED."name",
         "position" = EXCLUDED."position",
         "area" = EXCLUDED."area",
         "ip_range" = EXCLUDED."ip_range",
         "metadata" = EXCLUDED."metadata"
         returning "id", "name", "position", "area", "ip_range", "metadata"
      """)
    )
      .updateReturning(LocationRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<LocationRow> upsertBatch(
    Iterator<LocationRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
                values (?::uuid, ?, ?::point, ?::polygon, ?::inet, ?::jsonb)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                "position" = EXCLUDED."position",
                "area" = EXCLUDED."area",
                "ip_range" = EXCLUDED."ip_range",
                "metadata" = EXCLUDED."metadata"
                returning "id", "name", "position", "area", "ip_range", "metadata"
             """))
      .updateManyReturning(LocationRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<LocationRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table location_TEMP (like "frontpage"."location") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy location_TEMP("id", "name", "position", "area", "ip_range", "metadata") from stdin
      """), batchSize, unsaved, c, LocationRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
              select * from location_TEMP
              on conflict ("id")
              do update set
                "name" = EXCLUDED."name",
              "position" = EXCLUDED."position",
              "area" = EXCLUDED."area",
              "ip_range" = EXCLUDED."ip_range",
              "metadata" = EXCLUDED."metadata"
              ;
              drop table location_TEMP;""")).update().runUnchecked(c);
  };
}