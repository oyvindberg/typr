/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.location;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record LocationRepoMock(
  java.util.function.Function<LocationRowUnsaved, LocationRow> toRow,
  HashMap<LocationId, LocationRow> map
) implements LocationRepo {
  public LocationRepoMock(java.util.function.Function<LocationRowUnsaved, LocationRow> toRow) {
    this(toRow, new HashMap<LocationId, LocationRow>());
  };

  public LocationRepoMock withToRow(java.util.function.Function<LocationRowUnsaved, LocationRow> toRow) {
    return new LocationRepoMock(toRow, map);
  };

  public LocationRepoMock withMap(HashMap<LocationId, LocationRow> map) {
    return new LocationRepoMock(toRow, map);
  };

  public DeleteBuilder<LocationFields, LocationRow> delete() {
    return new DeleteBuilderMock<>(LocationFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    LocationId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    LocationId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public LocationRow insert(
    LocationRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public LocationRow insert(
    LocationRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<LocationRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<LocationRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<LocationFields, LocationRow> select() {
    return new SelectBuilderMock<>(LocationFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<LocationRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<LocationRow> selectById(
    LocationId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<LocationRow> selectByIds(
    LocationId[] ids,
    Connection c
  ) {
    var result = new ArrayList<LocationRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<LocationId, LocationRow> selectByIdsTracked(
    LocationId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.location.LocationRow row) -> row.id(), Function.identity()));
  };

  public UpdateBuilder<LocationFields, LocationRow> update() {
    return new UpdateBuilderMock<>(LocationFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    LocationRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public LocationRow upsert(
    LocationRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<LocationRow> upsertBatch(
    Iterator<LocationRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<LocationRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<LocationRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}