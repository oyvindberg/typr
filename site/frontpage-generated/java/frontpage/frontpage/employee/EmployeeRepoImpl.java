/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.employee;

import frontpage.frontpage.person.PersonId;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.Fragment.Literal;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;
import static typo.runtime.Fragment.interpolate;
import static typo.runtime.internal.stringInterpolator.str;

public class EmployeeRepoImpl implements EmployeeRepo {
  public DeleteBuilder<EmployeeFields, EmployeeRow> delete() {
    return DeleteBuilder.of("frontpage.employee", EmployeeFields.structure());
  };

  public Boolean deleteById(
    EmployeeId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
      delete from "frontpage"."employee" where "id" = 
      """),
      EmployeeId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0;
  };

  public Integer deleteByIds(
    EmployeeId[] ids,
    Connection c
  ) {
    return interpolate(
               typo.runtime.Fragment.lit("""
                  delete
                  from "frontpage"."employee"
                  where "id" = ANY("""),
               EmployeeId.pgTypeArray.encode(ids),
               typo.runtime.Fragment.lit(")")
             )
      .update()
      .runUnchecked(c);
  };

  public EmployeeRow insert(
    EmployeeRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."employee"("id", "person_id", "salary")
         values ("""),
      EmployeeId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PersonId.pgType.encode(unsaved.personId()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.numeric.opt().encode(unsaved.salary()),
      typo.runtime.Fragment.lit("""
         ::numeric)
         returning "id", "person_id", "salary"
      """)
    )
      .updateReturning(EmployeeRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public EmployeeRow insert(
    EmployeeRowUnsaved unsaved,
    Connection c
  ) {
    List<Literal> columns = new ArrayList<>();;
      List<Fragment> values = new ArrayList<>();;
      columns.add(Fragment.lit("\"person_id\""));
      values.add(interpolate(
        PersonId.pgType.encode(unsaved.personId()),
        typo.runtime.Fragment.lit("::uuid")
      ));
      columns.add(Fragment.lit("\"salary\""));
      values.add(interpolate(
        PgTypes.numeric.opt().encode(unsaved.salary()),
        typo.runtime.Fragment.lit("::numeric")
      ));
      unsaved.id().visit(
        () -> {},
        value -> {
          columns.add(Fragment.lit("\"id\""));
          values.add(interpolate(
            EmployeeId.pgType.encode(value),
            typo.runtime.Fragment.lit("::uuid")
          ));
        }
      );;
      Fragment q = interpolate(
        typo.runtime.Fragment.lit("""
        insert into "frontpage"."employee"(
        """),
        Fragment.comma(columns),
        typo.runtime.Fragment.lit("""
           )
           values ("""),
        Fragment.comma(values),
        typo.runtime.Fragment.lit("""
           )
           returning "id", "person_id", "salary"
        """)
      );;
    return q.updateReturning(EmployeeRow._rowParser.exactlyOne()).runUnchecked(c);
  };

  public Long insertStreaming(
    Iterator<EmployeeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."employee"("id", "person_id", "salary") FROM STDIN
    """), batchSize, unsaved, c, EmployeeRow.pgText);
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<EmployeeRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return streamingInsert.insertUnchecked(str("""
    COPY "frontpage"."employee"("person_id", "salary", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')
    """), batchSize, unsaved, c, EmployeeRowUnsaved.pgText);
  };

  public SelectBuilder<EmployeeFields, EmployeeRow> select() {
    return SelectBuilder.of("frontpage.employee", EmployeeFields.structure(), EmployeeRow._rowParser);
  };

  public List<EmployeeRow> selectAll(Connection c) {
    return interpolate(typo.runtime.Fragment.lit("""
       select "id", "person_id", "salary"
       from "frontpage"."employee"
    """)).as(EmployeeRow._rowParser.all()).runUnchecked(c);
  };

  public Optional<EmployeeRow> selectById(
    EmployeeId id,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "person_id", "salary"
         from "frontpage"."employee"
         where "id" = """),
      EmployeeId.pgType.encode(id),
      typo.runtime.Fragment.lit("")
    ).as(EmployeeRow._rowParser.first()).runUnchecked(c);
  };

  public List<EmployeeRow> selectByIds(
    EmployeeId[] ids,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "person_id", "salary"
         from "frontpage"."employee"
         where "id" = ANY("""),
      EmployeeId.pgTypeArray.encode(ids),
      typo.runtime.Fragment.lit(")")
    ).as(EmployeeRow._rowParser.all()).runUnchecked(c);
  };

  public Map<EmployeeId, EmployeeRow> selectByIdsTracked(
    EmployeeId[] ids,
    Connection c
  ) {
    Map<EmployeeId, EmployeeRow> ret = new HashMap<>();;
      selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  };

  public Optional<EmployeeRow> selectByUniquePersonId(
    PersonId personId,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         select "id", "person_id", "salary"
         from "frontpage"."employee"
         where "person_id" = """),
      PersonId.pgType.encode(personId),
      typo.runtime.Fragment.lit("""


      """)
    ).as(EmployeeRow._rowParser.first()).runUnchecked(c);
  };

  public UpdateBuilder<EmployeeFields, EmployeeRow> update() {
    return UpdateBuilder.of("frontpage.employee", EmployeeFields.structure(), EmployeeRow._rowParser.all());
  };

  public Boolean update(
    EmployeeRow row,
    Connection c
  ) {
    EmployeeId id = row.id();;
    return interpolate(
             typo.runtime.Fragment.lit("""
                update "frontpage"."employee"
                set "person_id" = """),
             PersonId.pgType.encode(row.personId()),
             typo.runtime.Fragment.lit("""
                ::uuid,
                "salary" = """),
             PgTypes.numeric.opt().encode(row.salary()),
             typo.runtime.Fragment.lit("""
                ::numeric
                where "id" = """),
             EmployeeId.pgType.encode(id),
             typo.runtime.Fragment.lit("")
           ).update().runUnchecked(c) > 0;
  };

  public EmployeeRow upsert(
    EmployeeRow unsaved,
    Connection c
  ) {
    return interpolate(
      typo.runtime.Fragment.lit("""
         insert into "frontpage"."employee"("id", "person_id", "salary")
         values ("""),
      EmployeeId.pgType.encode(unsaved.id()),
      typo.runtime.Fragment.lit("::uuid, "),
      PersonId.pgType.encode(unsaved.personId()),
      typo.runtime.Fragment.lit("::uuid, "),
      PgTypes.numeric.opt().encode(unsaved.salary()),
      typo.runtime.Fragment.lit("""
         ::numeric)
         on conflict ("id")
         do update set
           "person_id" = EXCLUDED."person_id",
         "salary" = EXCLUDED."salary"
         returning "id", "person_id", "salary"
      """)
    )
      .updateReturning(EmployeeRow._rowParser.exactlyOne())
      .runUnchecked(c);
  };

  public List<EmployeeRow> upsertBatch(
    Iterator<EmployeeRow> unsaved,
    Connection c
  ) {
    return interpolate(typo.runtime.Fragment.lit("""
                insert into "frontpage"."employee"("id", "person_id", "salary")
                values (?::uuid, ?::uuid, ?::numeric)
                on conflict ("id")
                do update set
                  "person_id" = EXCLUDED."person_id",
                "salary" = EXCLUDED."salary"
                returning "id", "person_id", "salary"
             """))
      .updateManyReturning(EmployeeRow._rowParser, unsaved)
      .runUnchecked(c);
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<EmployeeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table employee_TEMP (like "frontpage"."employee") on commit drop
      """)).update().runUnchecked(c);
      streamingInsert.insertUnchecked(str("""
      copy employee_TEMP("id", "person_id", "salary") from stdin
      """), batchSize, unsaved, c, EmployeeRow.pgText);
    return interpolate(typo.runtime.Fragment.lit("""
              insert into "frontpage"."employee"("id", "person_id", "salary")
              select * from employee_TEMP
              on conflict ("id")
              do update set
                "person_id" = EXCLUDED."person_id",
              "salary" = EXCLUDED."salary"
              ;
              drop table employee_TEMP;""")).update().runUnchecked(c);
  };
}