/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.order_item;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record OrderItemRepoMock(
  java.util.function.Function<OrderItemRowUnsaved, OrderItemRow> toRow,
  HashMap<OrderItemId, OrderItemRow> map
) implements OrderItemRepo {
  public OrderItemRepoMock(java.util.function.Function<OrderItemRowUnsaved, OrderItemRow> toRow) {
    this(toRow, new HashMap<OrderItemId, OrderItemRow>());
  };

  public OrderItemRepoMock withToRow(java.util.function.Function<OrderItemRowUnsaved, OrderItemRow> toRow) {
    return new OrderItemRepoMock(toRow, map);
  };

  public OrderItemRepoMock withMap(HashMap<OrderItemId, OrderItemRow> map) {
    return new OrderItemRepoMock(toRow, map);
  };

  public DeleteBuilder<OrderItemFields, OrderItemRow> delete() {
    return new DeleteBuilderMock<>(OrderItemFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  };

  public Boolean deleteById(
    OrderItemId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  };

  public Integer deleteByIds(
    OrderItemId[] ids,
    Connection c
  ) {
    var count = 0;
      for (var id : ids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public OrderItemRow insert(
    OrderItemRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
        throw new RuntimeException(str("id $unsaved.id() already exists"));
      };
      map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public OrderItemRow insert(
    OrderItemRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<OrderItemRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<OrderItemRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.id(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<OrderItemFields, OrderItemRow> select() {
    return new SelectBuilderMock<>(OrderItemFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<OrderItemRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<OrderItemRow> selectById(
    OrderItemId id,
    Connection c
  ) {
    return Optional.ofNullable(map.get(id));
  };

  public List<OrderItemRow> selectByIds(
    OrderItemId[] ids,
    Connection c
  ) {
    var result = new ArrayList<OrderItemRow>();
      for (var id : ids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<OrderItemId, OrderItemRow> selectByIdsTracked(
    OrderItemId[] ids,
    Connection c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((frontpage.frontpage.order_item.OrderItemRow row) -> row.id(), Function.identity()));
  };

  public UpdateBuilder<OrderItemFields, OrderItemRow> update() {
    return new UpdateBuilderMock<>(OrderItemFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    OrderItemRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.id(), row);
      };
    return shouldUpdate;
  };

  public OrderItemRow upsert(
    OrderItemRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  };

  public List<OrderItemRow> upsertBatch(
    Iterator<OrderItemRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<OrderItemRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<OrderItemRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.id(), row);
        count = count + 1;
      };
    return count;
  };
}