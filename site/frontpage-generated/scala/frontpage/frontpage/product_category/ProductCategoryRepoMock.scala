/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.product_category

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class ProductCategoryRepoMock(map: scala.collection.mutable.Map[ProductCategoryId, ProductCategoryRow] = scala.collection.mutable.Map.empty[ProductCategoryId, ProductCategoryRow]) extends ProductCategoryRepo {
  def delete: DeleteBuilder[ProductCategoryFields, ProductCategoryRow] = DeleteBuilderMock(DeleteParams.empty, ProductCategoryFields.structure, map)

  def deleteById(compositeId: ProductCategoryId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  def deleteByIds(compositeIds: Array[ProductCategoryId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: ProductCategoryRow)(using c: Connection): ProductCategoryRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  def insertStreaming(
    unsaved: Iterator[ProductCategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[ProductCategoryFields, ProductCategoryRow] = SelectBuilderMock(ProductCategoryFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[ProductCategoryRow] = map.values.toList

  def selectById(compositeId: ProductCategoryId)(using c: Connection): Option[ProductCategoryRow] = map.get(compositeId)

  def selectByIds(compositeIds: Array[ProductCategoryId])(using c: Connection): List[ProductCategoryRow] = compositeIds.flatMap(map.get).toList

  def selectByIdsTracked(compositeIds: Array[ProductCategoryId])(using c: Connection): Map[ProductCategoryId, ProductCategoryRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[ProductCategoryFields, ProductCategoryRow] = UpdateBuilderMock(UpdateParams.empty, ProductCategoryFields.structure, map)

  def upsert(unsaved: ProductCategoryRow)(using c: Connection): ProductCategoryRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[ProductCategoryRow])(using c: Connection): List[ProductCategoryRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[ProductCategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}