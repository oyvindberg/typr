/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage

import anorm.Column
import anorm.ParameterMetaData
import anorm.SqlMappingError
import anorm.ToStatement
import frontpage.Text
import java.sql.Types
import play.api.libs.json.JsError
import play.api.libs.json.JsSuccess
import play.api.libs.json.JsValue
import play.api.libs.json.Reads
import play.api.libs.json.Writes

/** Enum `frontpage.order_status`
 *  - pending
 *  - active
 *  - shipped
 *  - cancelled
 */

sealed abstract class OrderStatus(val value: java.lang.String)

object OrderStatus {
  given arrayColumn: Column[Array[OrderStatus]] = Column.columnToArray[String](using Column.columnToString, implicitly).map(_.map(OrderStatus.force))

  given column: Column[OrderStatus] = Column.columnToString.mapResult(str => OrderStatus(str).left.map(SqlMappingError.apply))

  given toStatement: ToStatement[OrderStatus] = ToStatement.stringToStatement.contramap(_.value)

  given arrayToStatement: ToStatement[Array[OrderStatus]] = ToStatement[Array[OrderStatus]]((ps, i, arr) => ps.setArray(i, ps.getConnection.createArrayOf("frontpage.order_status", arr.map[AnyRef](_.value))))

  given parameterMetadata: ParameterMetaData[OrderStatus] = {
    new ParameterMetaData[OrderStatus] {
      override def sqlType: String = "frontpage.order_status"
      override def jdbcType: Int = Types.OTHER
    }
  }

  given pgText: Text[OrderStatus] = {
    new Text[OrderStatus] {
      override def unsafeEncode(v: OrderStatus, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: OrderStatus, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given reads: Reads[OrderStatus] = Reads[OrderStatus]{(value: JsValue) => value.validate(Reads.StringReads).flatMap(str => OrderStatus(str).fold(JsError.apply, JsSuccess(_)))}

  given writes: Writes[OrderStatus] = Writes[OrderStatus](value => Writes.StringWrites.writes(value.value))
  def apply(str: java.lang.String): scala.Either[java.lang.String, OrderStatus] =
    ByName.get(str).toRight(s"'$str' does not match any of the following legal values: $Names")
  def force(str: java.lang.String): OrderStatus =
    apply(str) match {
      case scala.Left(msg) => sys.error(msg)
      case scala.Right(value) => value
    }
  case object pending extends OrderStatus("pending")

  case object active extends OrderStatus("active")

  case object shipped extends OrderStatus("shipped")

  case object cancelled extends OrderStatus("cancelled")
  val All: scala.List[OrderStatus] = scala.List(pending, active, shipped, cancelled)
  val Names: java.lang.String = All.map(_.value).mkString(", ")
  val ByName: scala.collection.immutable.Map[java.lang.String, OrderStatus] = All.map(x => (x.value, x)).toMap
}