/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission

import frontpage.Text
import frontpage.customtypes.Defaulted
import frontpage.customtypes.Defaulted.UseDefault
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `frontpage.permission` which has not been persisted yet */
case class PermissionRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[PermissionId] = new UseDefault()
) {
  def toRow(idDefault: => PermissionId): PermissionRow = new PermissionRow(id = id.getOrElse(idDefault), name = name)
}

object PermissionRowUnsaved {
  given pgText: Text[PermissionRowUnsaved] = {
    Text.instance[PermissionRowUnsaved]{ (row, sb) =>
      Text.stringInstance.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using PermissionId.pgText).unsafeEncode(row.id, sb)
    }
  }

  given reads: Reads[PermissionRowUnsaved] = {
    Reads[PermissionRowUnsaved](json => JsResult.fromTry(
        Try(
          PermissionRowUnsaved(
            name = json.\("name").as(Reads.StringReads),
            id = json.\("id").as(Defaulted.reads(using PermissionId.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[PermissionRowUnsaved] = {
    OWrites[PermissionRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "name" -> Writes.StringWrites.writes(o.name),
        "id" -> Defaulted.writes(using PermissionId.writes).writes(o.id)
      ))
    )
  }
}