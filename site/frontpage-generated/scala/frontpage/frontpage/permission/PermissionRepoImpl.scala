/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class PermissionRepoImpl extends PermissionRepo {
  def delete: DeleteBuilder[PermissionFields, PermissionRow] = DeleteBuilder.of(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.rowParser(1).*)

  def deleteById(id: PermissionId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."permission" where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[PermissionId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."permission"
    where "id" = ANY(${ParameterValue(ids, null, PermissionId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: PermissionRow)(using c: Connection): PermissionRow = {
  SQL"""insert into "frontpage"."permission"("id", "name")
    values (${ParameterValue(unsaved.id, null, PermissionId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
    returning "id", "name"
    """
    .executeInsert(PermissionRow.rowParser(1).single)
  }

  def insert(unsaved: PermissionRowUnsaved)(using c: Connection): PermissionRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, PermissionId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."permission" default values
      returning "id", "name"
      """
        .executeInsert(PermissionRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."permission"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "name"
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(PermissionRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[PermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."permission"("id", "name") FROM STDIN""", batchSize, unsaved)(using PermissionRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[PermissionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."permission"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using PermissionRowUnsaved.pgText, c)

  def select: SelectBuilder[PermissionFields, PermissionRow] = SelectBuilder.of(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.rowParser)

  def selectAll(using c: Connection): List[PermissionRow] = {
    SQL"""select "id", "name"
    from "frontpage"."permission"
    """.as(PermissionRow.rowParser(1).*)
  }

  def selectById(id: PermissionId)(using c: Connection): Option[PermissionRow] = {
    SQL"""select "id", "name"
    from "frontpage"."permission"
    where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}
    """.as(PermissionRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[PermissionId])(using c: Connection): List[PermissionRow] = {
    SQL"""select "id", "name"
    from "frontpage"."permission"
    where "id" = ANY(${ParameterValue(ids, null, PermissionId.arrayToStatement)})
    """.as(PermissionRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[PermissionId])(using c: Connection): Map[PermissionId, PermissionRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniqueName(name: String)(using c: Connection): Option[PermissionRow] = {
    SQL"""select "id", "name"
    from "frontpage"."permission"
    where "name" = ${ParameterValue(name, null, ToStatement.stringToStatement)}
    """.as(PermissionRow.rowParser(1).singleOpt)
  }

  def update: UpdateBuilder[PermissionFields, PermissionRow] = UpdateBuilder.of(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.rowParser(1).*)

  def update(row: PermissionRow)(using c: Connection): Option[PermissionRow] = {
    val id = row.id
    SQL"""update "frontpage"."permission"
    set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
    where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}
    returning "id", "name"
    """.executeInsert(PermissionRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: PermissionRow)(using c: Connection): PermissionRow = {
  SQL"""insert into "frontpage"."permission"("id", "name")
    values (
      ${ParameterValue(unsaved.id, null, PermissionId.toStatement)}::uuid,
    ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
    )
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    returning "id", "name"
    """
    .executeInsert(PermissionRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[PermissionRow])(using c: Connection): List[PermissionRow] = {
    def toNamedParameter(row: PermissionRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, PermissionId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."permission"("id", "name")
            values ({id}::uuid, {name})
            on conflict ("id")
            do update set
              "name" = EXCLUDED."name"
            returning "id", "name"
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(PermissionRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[PermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table permission_TEMP (like "frontpage"."permission") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy permission_TEMP("id", "name") from stdin""", batchSize, unsaved)(using PermissionRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."permission"("id", "name")
    select * from permission_TEMP
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    ;
    drop table permission_TEMP;""".executeUpdate()
  }
}