/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class PermissionRepoMock(
  toRow: PermissionRowUnsaved => PermissionRow,
  map: scala.collection.mutable.Map[PermissionId, PermissionRow] = scala.collection.mutable.Map.empty[PermissionId, PermissionRow]
) extends PermissionRepo {
  def delete: DeleteBuilder[PermissionFields, PermissionRow] = DeleteBuilderMock(DeleteParams.empty, PermissionFields.structure, map)

  def deleteById(id: PermissionId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[PermissionId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: PermissionRow)(using c: Connection): PermissionRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: PermissionRowUnsaved)(using c: Connection): PermissionRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[PermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[PermissionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[PermissionFields, PermissionRow] = SelectBuilderMock(PermissionFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[PermissionRow] = map.values.toList

  def selectById(id: PermissionId)(using c: Connection): Option[PermissionRow] = map.get(id)

  def selectByIds(ids: Array[PermissionId])(using c: Connection): List[PermissionRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[PermissionId])(using c: Connection): Map[PermissionId, PermissionRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniqueName(name: String)(using c: Connection): Option[PermissionRow] = map.values.find(v => name == v.name)

  def update: UpdateBuilder[PermissionFields, PermissionRow] = UpdateBuilderMock(UpdateParams.empty, PermissionFields.structure, map)

  def update(row: PermissionRow)(using c: Connection): Option[PermissionRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: PermissionRow)(using c: Connection): PermissionRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[PermissionRow])(using c: Connection): List[PermissionRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[PermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}