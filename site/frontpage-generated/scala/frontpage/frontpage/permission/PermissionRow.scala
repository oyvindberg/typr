/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.permission

import anorm.Column
import anorm.RowParser
import anorm.Success
import frontpage.Text
import frontpage.customtypes.Defaulted
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: frontpage.permission
 * Primary key: id
 */
case class PermissionRow(
  /** Default: gen_random_uuid() */
  id: PermissionId,
  name: String
) {
  def toUnsavedRow(id: Defaulted[PermissionId]): PermissionRowUnsaved = new PermissionRowUnsaved(name, id)
}

object PermissionRow {
  given pgText: Text[PermissionRow] = {
    Text.instance[PermissionRow]{ (row, sb) =>
      PermissionId.pgText.unsafeEncode(row.id, sb)
      sb.append(Text.DELIMETER)
      Text.stringInstance.unsafeEncode(row.name, sb)
    }
  }

  given reads: Reads[PermissionRow] = {
    Reads[PermissionRow](json => JsResult.fromTry(
        Try(
          PermissionRow(
            id = json.\("id").as(PermissionId.reads),
            name = json.\("name").as(Reads.StringReads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[PermissionRow] = {
    RowParser[PermissionRow] { row =>
      Success(
        PermissionRow(
          id = row(idx + 0)(using PermissionId.column),
          name = row(idx + 1)(using Column.columnToString)
        )
      )
    }
  }

  given writes: OWrites[PermissionRow] = {
    OWrites[PermissionRow](o =>
      new JsObject(ListMap[String, JsValue](
        "id" -> PermissionId.writes.writes(o.id),
        "name" -> Writes.StringWrites.writes(o.name)
      ))
    )
  }
}