/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.employee

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterMetaData
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.frontpage.person.PersonId
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class EmployeeRepoImpl extends EmployeeRepo {
  def delete: DeleteBuilder[EmployeeFields, EmployeeRow] = DeleteBuilder.of(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.rowParser(1).*)

  def deleteById(id: EmployeeId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."employee" where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[EmployeeId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."employee"
    where "id" = ANY(${ParameterValue(ids, null, EmployeeId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: EmployeeRow)(using c: Connection): EmployeeRow = {
  SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
    values (${ParameterValue(unsaved.id, null, EmployeeId.toStatement)}::uuid, ${ParameterValue(unsaved.personId, null, PersonId.toStatement)}::uuid, ${ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(using ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric)
    returning "id", "person_id", "salary"
    """
    .executeInsert(EmployeeRow.rowParser(1).single)
  }

  def insert(unsaved: EmployeeRowUnsaved)(using c: Connection): EmployeeRow = {
    val namedParameters = List(
      Some((NamedParameter("person_id", ParameterValue(unsaved.personId, null, PersonId.toStatement)), "::uuid")),
      Some((NamedParameter("salary", ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(using ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))), "::numeric")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, EmployeeId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."employee" default values
      returning "id", "person_id", "salary"
      """
        .executeInsert(EmployeeRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."employee"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "person_id", "salary"
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(EmployeeRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[EmployeeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."employee"("id", "person_id", "salary") FROM STDIN""", batchSize, unsaved)(using EmployeeRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[EmployeeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."employee"("person_id", "salary", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using EmployeeRowUnsaved.pgText, c)

  def select: SelectBuilder[EmployeeFields, EmployeeRow] = SelectBuilder.of(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.rowParser)

  def selectAll(using c: Connection): List[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
    from "frontpage"."employee"
    """.as(EmployeeRow.rowParser(1).*)
  }

  def selectById(id: EmployeeId)(using c: Connection): Option[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
    from "frontpage"."employee"
    where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}
    """.as(EmployeeRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[EmployeeId])(using c: Connection): List[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
    from "frontpage"."employee"
    where "id" = ANY(${ParameterValue(ids, null, EmployeeId.arrayToStatement)})
    """.as(EmployeeRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[EmployeeId])(using c: Connection): Map[EmployeeId, EmployeeRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniquePersonId(personId: PersonId)(using c: Connection): Option[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
    from "frontpage"."employee"
    where "person_id" = ${ParameterValue(personId, null, PersonId.toStatement)}
    """.as(EmployeeRow.rowParser(1).singleOpt)
  }

  def update: UpdateBuilder[EmployeeFields, EmployeeRow] = UpdateBuilder.of(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.rowParser(1).*)

  def update(row: EmployeeRow)(using c: Connection): Option[EmployeeRow] = {
    val id = row.id
    SQL"""update "frontpage"."employee"
    set "person_id" = ${ParameterValue(row.personId, null, PersonId.toStatement)}::uuid,
    "salary" = ${ParameterValue(row.salary, null, ToStatement.optionToStatement(using ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric
    where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}
    returning "id", "person_id", "salary"
    """.executeInsert(EmployeeRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: EmployeeRow)(using c: Connection): EmployeeRow = {
  SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
    values (
      ${ParameterValue(unsaved.id, null, EmployeeId.toStatement)}::uuid,
    ${ParameterValue(unsaved.personId, null, PersonId.toStatement)}::uuid,
    ${ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(using ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric
    )
    on conflict ("id")
    do update set
      "person_id" = EXCLUDED."person_id",
    "salary" = EXCLUDED."salary"
    returning "id", "person_id", "salary"
    """
    .executeInsert(EmployeeRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[EmployeeRow])(using c: Connection): List[EmployeeRow] = {
    def toNamedParameter(row: EmployeeRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, EmployeeId.toStatement)),
      NamedParameter("person_id", ParameterValue(row.personId, null, PersonId.toStatement)),
      NamedParameter("salary", ParameterValue(row.salary, null, ToStatement.optionToStatement(using ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."employee"("id", "person_id", "salary")
            values ({id}::uuid, {person_id}::uuid, {salary}::numeric)
            on conflict ("id")
            do update set
              "person_id" = EXCLUDED."person_id",
            "salary" = EXCLUDED."salary"
            returning "id", "person_id", "salary"
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(EmployeeRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[EmployeeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table employee_TEMP (like "frontpage"."employee") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy employee_TEMP("id", "person_id", "salary") from stdin""", batchSize, unsaved)(using EmployeeRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
    select * from employee_TEMP
    on conflict ("id")
    do update set
      "person_id" = EXCLUDED."person_id",
    "salary" = EXCLUDED."salary"
    ;
    drop table employee_TEMP;""".executeUpdate()
  }
}