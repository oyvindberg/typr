/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.employee

import frontpage.frontpage.person.PersonId
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class EmployeeRepoMock(
  toRow: EmployeeRowUnsaved => EmployeeRow,
  map: scala.collection.mutable.Map[EmployeeId, EmployeeRow] = scala.collection.mutable.Map.empty[EmployeeId, EmployeeRow]
) extends EmployeeRepo {
  def delete: DeleteBuilder[EmployeeFields, EmployeeRow] = DeleteBuilderMock(DeleteParams.empty, EmployeeFields.structure, map)

  def deleteById(id: EmployeeId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[EmployeeId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: EmployeeRow)(using c: Connection): EmployeeRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: EmployeeRowUnsaved)(using c: Connection): EmployeeRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[EmployeeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[EmployeeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[EmployeeFields, EmployeeRow] = SelectBuilderMock(EmployeeFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[EmployeeRow] = map.values.toList

  def selectById(id: EmployeeId)(using c: Connection): Option[EmployeeRow] = map.get(id)

  def selectByIds(ids: Array[EmployeeId])(using c: Connection): List[EmployeeRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[EmployeeId])(using c: Connection): Map[EmployeeId, EmployeeRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniquePersonId(personId: PersonId)(using c: Connection): Option[EmployeeRow] = map.values.find(v => personId == v.personId)

  def update: UpdateBuilder[EmployeeFields, EmployeeRow] = UpdateBuilderMock(UpdateParams.empty, EmployeeFields.structure, map)

  def update(row: EmployeeRow)(using c: Connection): Option[EmployeeRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: EmployeeRow)(using c: Connection): EmployeeRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[EmployeeRow])(using c: Connection): List[EmployeeRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[EmployeeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}