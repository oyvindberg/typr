/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.order_item

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.customtypes.TypoLocalDateTime
import frontpage.frontpage.order.OrderId
import frontpage.frontpage.product.ProductId
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class OrderItemRepoImpl extends OrderItemRepo {
  def delete: DeleteBuilder[OrderItemFields, OrderItemRow] = DeleteBuilder.of(""""frontpage"."order_item"""", OrderItemFields.structure, OrderItemRow.rowParser(1).*)

  def deleteById(id: OrderItemId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."order_item" where "id" = ${ParameterValue(id, null, OrderItemId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[OrderItemId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."order_item"
    where "id" = ANY(${ParameterValue(ids, null, OrderItemId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: OrderItemRow)(using c: Connection): OrderItemRow = {
  SQL"""insert into "frontpage"."order_item"("id", "order_id", "product_id", "quantity", "price", "shipped_at")
    values (${ParameterValue(unsaved.id, null, OrderItemId.toStatement)}::uuid, ${ParameterValue(unsaved.orderId, null, ToStatement.optionToStatement(using OrderId.toStatement, OrderId.parameterMetadata))}::uuid, ${ParameterValue(unsaved.productId, null, ToStatement.optionToStatement(using ProductId.toStatement, ProductId.parameterMetadata))}::uuid, ${ParameterValue(unsaved.quantity, null, ToStatement.intToStatement)}::int4, ${ParameterValue(unsaved.price, null, ToStatement.scalaBigDecimalToStatement)}::numeric, ${ParameterValue(unsaved.shippedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp)
    returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    """
    .executeInsert(OrderItemRow.rowParser(1).single)
  }

  def insert(unsaved: OrderItemRowUnsaved)(using c: Connection): OrderItemRow = {
    val namedParameters = List(
      Some((NamedParameter("order_id", ParameterValue(unsaved.orderId, null, ToStatement.optionToStatement(using OrderId.toStatement, OrderId.parameterMetadata))), "::uuid")),
      Some((NamedParameter("product_id", ParameterValue(unsaved.productId, null, ToStatement.optionToStatement(using ProductId.toStatement, ProductId.parameterMetadata))), "::uuid")),
      Some((NamedParameter("quantity", ParameterValue(unsaved.quantity, null, ToStatement.intToStatement)), "::int4")),
      Some((NamedParameter("price", ParameterValue(unsaved.price, null, ToStatement.scalaBigDecimalToStatement)), "::numeric")),
      Some((NamedParameter("shipped_at", ParameterValue(unsaved.shippedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))), "::timestamp")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, OrderItemId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."order_item" default values
      returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
      """
        .executeInsert(OrderItemRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."order_item"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(OrderItemRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[OrderItemRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."order_item"("id", "order_id", "product_id", "quantity", "price", "shipped_at") FROM STDIN""", batchSize, unsaved)(using OrderItemRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[OrderItemRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."order_item"("order_id", "product_id", "quantity", "price", "shipped_at", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using OrderItemRowUnsaved.pgText, c)

  def select: SelectBuilder[OrderItemFields, OrderItemRow] = SelectBuilder.of(""""frontpage"."order_item"""", OrderItemFields.structure, OrderItemRow.rowParser)

  def selectAll(using c: Connection): List[OrderItemRow] = {
    SQL"""select "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    from "frontpage"."order_item"
    """.as(OrderItemRow.rowParser(1).*)
  }

  def selectById(id: OrderItemId)(using c: Connection): Option[OrderItemRow] = {
    SQL"""select "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    from "frontpage"."order_item"
    where "id" = ${ParameterValue(id, null, OrderItemId.toStatement)}
    """.as(OrderItemRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[OrderItemId])(using c: Connection): List[OrderItemRow] = {
    SQL"""select "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    from "frontpage"."order_item"
    where "id" = ANY(${ParameterValue(ids, null, OrderItemId.arrayToStatement)})
    """.as(OrderItemRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[OrderItemId])(using c: Connection): Map[OrderItemId, OrderItemRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[OrderItemFields, OrderItemRow] = UpdateBuilder.of(""""frontpage"."order_item"""", OrderItemFields.structure, OrderItemRow.rowParser(1).*)

  def update(row: OrderItemRow)(using c: Connection): Option[OrderItemRow] = {
    val id = row.id
    SQL"""update "frontpage"."order_item"
    set "order_id" = ${ParameterValue(row.orderId, null, ToStatement.optionToStatement(using OrderId.toStatement, OrderId.parameterMetadata))}::uuid,
    "product_id" = ${ParameterValue(row.productId, null, ToStatement.optionToStatement(using ProductId.toStatement, ProductId.parameterMetadata))}::uuid,
    "quantity" = ${ParameterValue(row.quantity, null, ToStatement.intToStatement)}::int4,
    "price" = ${ParameterValue(row.price, null, ToStatement.scalaBigDecimalToStatement)}::numeric,
    "shipped_at" = ${ParameterValue(row.shippedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    where "id" = ${ParameterValue(id, null, OrderItemId.toStatement)}
    returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    """.executeInsert(OrderItemRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: OrderItemRow)(using c: Connection): OrderItemRow = {
  SQL"""insert into "frontpage"."order_item"("id", "order_id", "product_id", "quantity", "price", "shipped_at")
    values (
      ${ParameterValue(unsaved.id, null, OrderItemId.toStatement)}::uuid,
    ${ParameterValue(unsaved.orderId, null, ToStatement.optionToStatement(using OrderId.toStatement, OrderId.parameterMetadata))}::uuid,
    ${ParameterValue(unsaved.productId, null, ToStatement.optionToStatement(using ProductId.toStatement, ProductId.parameterMetadata))}::uuid,
    ${ParameterValue(unsaved.quantity, null, ToStatement.intToStatement)}::int4,
    ${ParameterValue(unsaved.price, null, ToStatement.scalaBigDecimalToStatement)}::numeric,
    ${ParameterValue(unsaved.shippedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    )
    on conflict ("id")
    do update set
      "order_id" = EXCLUDED."order_id",
    "product_id" = EXCLUDED."product_id",
    "quantity" = EXCLUDED."quantity",
    "price" = EXCLUDED."price",
    "shipped_at" = EXCLUDED."shipped_at"
    returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
    """
    .executeInsert(OrderItemRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[OrderItemRow])(using c: Connection): List[OrderItemRow] = {
    def toNamedParameter(row: OrderItemRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, OrderItemId.toStatement)),
      NamedParameter("order_id", ParameterValue(row.orderId, null, ToStatement.optionToStatement(using OrderId.toStatement, OrderId.parameterMetadata))),
      NamedParameter("product_id", ParameterValue(row.productId, null, ToStatement.optionToStatement(using ProductId.toStatement, ProductId.parameterMetadata))),
      NamedParameter("quantity", ParameterValue(row.quantity, null, ToStatement.intToStatement)),
      NamedParameter("price", ParameterValue(row.price, null, ToStatement.scalaBigDecimalToStatement)),
      NamedParameter("shipped_at", ParameterValue(row.shippedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."order_item"("id", "order_id", "product_id", "quantity", "price", "shipped_at")
            values ({id}::uuid, {order_id}::uuid, {product_id}::uuid, {quantity}::int4, {price}::numeric, {shipped_at}::timestamp)
            on conflict ("id")
            do update set
              "order_id" = EXCLUDED."order_id",
            "product_id" = EXCLUDED."product_id",
            "quantity" = EXCLUDED."quantity",
            "price" = EXCLUDED."price",
            "shipped_at" = EXCLUDED."shipped_at"
            returning "id", "order_id", "product_id", "quantity", "price", "shipped_at"::text
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(OrderItemRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[OrderItemRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table order_item_TEMP (like "frontpage"."order_item") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy order_item_TEMP("id", "order_id", "product_id", "quantity", "price", "shipped_at") from stdin""", batchSize, unsaved)(using OrderItemRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."order_item"("id", "order_id", "product_id", "quantity", "price", "shipped_at")
    select * from order_item_TEMP
    on conflict ("id")
    do update set
      "order_id" = EXCLUDED."order_id",
    "product_id" = EXCLUDED."product_id",
    "quantity" = EXCLUDED."quantity",
    "price" = EXCLUDED."price",
    "shipped_at" = EXCLUDED."shipped_at"
    ;
    drop table order_item_TEMP;""".executeUpdate()
  }
}