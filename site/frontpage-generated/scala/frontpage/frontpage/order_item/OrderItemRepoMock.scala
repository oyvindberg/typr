/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.order_item

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class OrderItemRepoMock(
  toRow: OrderItemRowUnsaved => OrderItemRow,
  map: scala.collection.mutable.Map[OrderItemId, OrderItemRow] = scala.collection.mutable.Map.empty[OrderItemId, OrderItemRow]
) extends OrderItemRepo {
  def delete: DeleteBuilder[OrderItemFields, OrderItemRow] = DeleteBuilderMock(DeleteParams.empty, OrderItemFields.structure, map)

  def deleteById(id: OrderItemId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[OrderItemId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: OrderItemRow)(using c: Connection): OrderItemRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: OrderItemRowUnsaved)(using c: Connection): OrderItemRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[OrderItemRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[OrderItemRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[OrderItemFields, OrderItemRow] = SelectBuilderMock(OrderItemFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[OrderItemRow] = map.values.toList

  def selectById(id: OrderItemId)(using c: Connection): Option[OrderItemRow] = map.get(id)

  def selectByIds(ids: Array[OrderItemId])(using c: Connection): List[OrderItemRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[OrderItemId])(using c: Connection): Map[OrderItemId, OrderItemRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[OrderItemFields, OrderItemRow] = UpdateBuilderMock(UpdateParams.empty, OrderItemFields.structure, map)

  def update(row: OrderItemRow)(using c: Connection): Option[OrderItemRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: OrderItemRow)(using c: Connection): OrderItemRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[OrderItemRow])(using c: Connection): List[OrderItemRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[OrderItemRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}