/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user_permission

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.customtypes.TypoLocalDateTime
import frontpage.frontpage.permission.PermissionId
import frontpage.frontpage.user.UserId
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class UserPermissionRepoImpl extends UserPermissionRepo {
  def delete: DeleteBuilder[UserPermissionFields, UserPermissionRow] = DeleteBuilder.of(""""frontpage"."user_permission"""", UserPermissionFields.structure, UserPermissionRow.rowParser(1).*)

  def deleteById(compositeId: UserPermissionId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."user_permission" where "user_id" = ${ParameterValue(compositeId.userId, null, UserId.toStatement)} AND "permission_id" = ${ParameterValue(compositeId.permissionId, null, PermissionId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(compositeIds: Array[UserPermissionId])(using c: Connection): Int = {
    val userId = compositeIds.map(_.userId)
    val permissionId = compositeIds.map(_.permissionId)
    SQL"""delete
    from "frontpage"."user_permission"
    where ("user_id", "permission_id")
    in (select unnest(${ParameterValue(userId, null, UserId.arrayToStatement)}), unnest(${ParameterValue(permissionId, null, PermissionId.arrayToStatement)}))
    """.executeUpdate()
  }

  def insert(unsaved: UserPermissionRow)(using c: Connection): UserPermissionRow = {
  SQL"""insert into "frontpage"."user_permission"("user_id", "permission_id", "granted_at")
    values (${ParameterValue(unsaved.userId, null, UserId.toStatement)}::uuid, ${ParameterValue(unsaved.permissionId, null, PermissionId.toStatement)}::uuid, ${ParameterValue(unsaved.grantedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp)
    returning "user_id", "permission_id", "granted_at"::text
    """
    .executeInsert(UserPermissionRow.rowParser(1).single)
  }

  def insert(unsaved: UserPermissionRowUnsaved)(using c: Connection): UserPermissionRow = {
    val namedParameters = List(
      Some((NamedParameter("user_id", ParameterValue(unsaved.userId, null, UserId.toStatement)), "::uuid")),
      Some((NamedParameter("permission_id", ParameterValue(unsaved.permissionId, null, PermissionId.toStatement)), "::uuid")),
      unsaved.grantedAt match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("granted_at", ParameterValue(value, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))), "::timestamp"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."user_permission" default values
      returning "user_id", "permission_id", "granted_at"::text
      """
        .executeInsert(UserPermissionRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."user_permission"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "user_id", "permission_id", "granted_at"::text
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(UserPermissionRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[UserPermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."user_permission"("user_id", "permission_id", "granted_at") FROM STDIN""", batchSize, unsaved)(using UserPermissionRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[UserPermissionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."user_permission"("user_id", "permission_id", "granted_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using UserPermissionRowUnsaved.pgText, c)

  def select: SelectBuilder[UserPermissionFields, UserPermissionRow] = SelectBuilder.of(""""frontpage"."user_permission"""", UserPermissionFields.structure, UserPermissionRow.rowParser)

  def selectAll(using c: Connection): List[UserPermissionRow] = {
    SQL"""select "user_id", "permission_id", "granted_at"::text
    from "frontpage"."user_permission"
    """.as(UserPermissionRow.rowParser(1).*)
  }

  def selectById(compositeId: UserPermissionId)(using c: Connection): Option[UserPermissionRow] = {
    SQL"""select "user_id", "permission_id", "granted_at"::text
    from "frontpage"."user_permission"
    where "user_id" = ${ParameterValue(compositeId.userId, null, UserId.toStatement)} AND "permission_id" = ${ParameterValue(compositeId.permissionId, null, PermissionId.toStatement)}
    """.as(UserPermissionRow.rowParser(1).singleOpt)
  }

  def selectByIds(compositeIds: Array[UserPermissionId])(using c: Connection): List[UserPermissionRow] = {
    val userId = compositeIds.map(_.userId)
    val permissionId = compositeIds.map(_.permissionId)
    SQL"""select "user_id", "permission_id", "granted_at"::text
    from "frontpage"."user_permission"
    where ("user_id", "permission_id")
    in (select unnest(${ParameterValue(userId, null, UserId.arrayToStatement)}), unnest(${ParameterValue(permissionId, null, PermissionId.arrayToStatement)}))
    """.as(UserPermissionRow.rowParser(1).*)
  }

  def selectByIdsTracked(compositeIds: Array[UserPermissionId])(using c: Connection): Map[UserPermissionId, UserPermissionRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[UserPermissionFields, UserPermissionRow] = UpdateBuilder.of(""""frontpage"."user_permission"""", UserPermissionFields.structure, UserPermissionRow.rowParser(1).*)

  def update(row: UserPermissionRow)(using c: Connection): Option[UserPermissionRow] = {
    val compositeId = row.compositeId
    SQL"""update "frontpage"."user_permission"
    set "granted_at" = ${ParameterValue(row.grantedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    where "user_id" = ${ParameterValue(compositeId.userId, null, UserId.toStatement)} AND "permission_id" = ${ParameterValue(compositeId.permissionId, null, PermissionId.toStatement)}
    returning "user_id", "permission_id", "granted_at"::text
    """.executeInsert(UserPermissionRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: UserPermissionRow)(using c: Connection): UserPermissionRow = {
  SQL"""insert into "frontpage"."user_permission"("user_id", "permission_id", "granted_at")
    values (
      ${ParameterValue(unsaved.userId, null, UserId.toStatement)}::uuid,
    ${ParameterValue(unsaved.permissionId, null, PermissionId.toStatement)}::uuid,
    ${ParameterValue(unsaved.grantedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    )
    on conflict ("user_id", "permission_id")
    do update set
      "granted_at" = EXCLUDED."granted_at"
    returning "user_id", "permission_id", "granted_at"::text
    """
    .executeInsert(UserPermissionRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[UserPermissionRow])(using c: Connection): List[UserPermissionRow] = {
    def toNamedParameter(row: UserPermissionRow): List[NamedParameter] = List(
      NamedParameter("user_id", ParameterValue(row.userId, null, UserId.toStatement)),
      NamedParameter("permission_id", ParameterValue(row.permissionId, null, PermissionId.toStatement)),
      NamedParameter("granted_at", ParameterValue(row.grantedAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."user_permission"("user_id", "permission_id", "granted_at")
            values ({user_id}::uuid, {permission_id}::uuid, {granted_at}::timestamp)
            on conflict ("user_id", "permission_id")
            do update set
              "granted_at" = EXCLUDED."granted_at"
            returning "user_id", "permission_id", "granted_at"::text
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(UserPermissionRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[UserPermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table user_permission_TEMP (like "frontpage"."user_permission") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy user_permission_TEMP("user_id", "permission_id", "granted_at") from stdin""", batchSize, unsaved)(using UserPermissionRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."user_permission"("user_id", "permission_id", "granted_at")
    select * from user_permission_TEMP
    on conflict ("user_id", "permission_id")
    do update set
      "granted_at" = EXCLUDED."granted_at"
    ;
    drop table user_permission_TEMP;""".executeUpdate()
  }
}