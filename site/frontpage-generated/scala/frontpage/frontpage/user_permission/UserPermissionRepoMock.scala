/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user_permission

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class UserPermissionRepoMock(
  toRow: UserPermissionRowUnsaved => UserPermissionRow,
  map: scala.collection.mutable.Map[UserPermissionId, UserPermissionRow] = scala.collection.mutable.Map.empty[UserPermissionId, UserPermissionRow]
) extends UserPermissionRepo {
  def delete: DeleteBuilder[UserPermissionFields, UserPermissionRow] = DeleteBuilderMock(DeleteParams.empty, UserPermissionFields.structure, map)

  def deleteById(compositeId: UserPermissionId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  def deleteByIds(compositeIds: Array[UserPermissionId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: UserPermissionRow)(using c: Connection): UserPermissionRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  def insert(unsaved: UserPermissionRowUnsaved)(using c: Connection): UserPermissionRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[UserPermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[UserPermissionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[UserPermissionFields, UserPermissionRow] = SelectBuilderMock(UserPermissionFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[UserPermissionRow] = map.values.toList

  def selectById(compositeId: UserPermissionId)(using c: Connection): Option[UserPermissionRow] = map.get(compositeId)

  def selectByIds(compositeIds: Array[UserPermissionId])(using c: Connection): List[UserPermissionRow] = compositeIds.flatMap(map.get).toList

  def selectByIdsTracked(compositeIds: Array[UserPermissionId])(using c: Connection): Map[UserPermissionId, UserPermissionRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[UserPermissionFields, UserPermissionRow] = UpdateBuilderMock(UpdateParams.empty, UserPermissionFields.structure, map)

  def update(row: UserPermissionRow)(using c: Connection): Option[UserPermissionRow] = {
    map.get(row.compositeId).map { _ =>
      map.put(row.compositeId, row): @nowarn
      row
    }
  }

  def upsert(unsaved: UserPermissionRow)(using c: Connection): UserPermissionRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[UserPermissionRow])(using c: Connection): List[UserPermissionRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[UserPermissionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}