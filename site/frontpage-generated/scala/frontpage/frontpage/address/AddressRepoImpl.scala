/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.address

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class AddressRepoImpl extends AddressRepo {
  def delete: DeleteBuilder[AddressFields, AddressRow] = DeleteBuilder.of(""""frontpage"."address"""", AddressFields.structure, AddressRow.rowParser(1).*)

  def deleteById(id: AddressId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."address" where "id" = ${ParameterValue(id, null, AddressId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[AddressId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."address"
    where "id" = ANY(${ParameterValue(ids, null, AddressId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: AddressRow)(using c: Connection): AddressRow = {
  SQL"""insert into "frontpage"."address"("id", "city", "country")
    values (${ParameterValue(unsaved.id, null, AddressId.toStatement)}::uuid, ${ParameterValue(unsaved.city, null, ToStatement.stringToStatement)}, ${ParameterValue(unsaved.country, null, ToStatement.stringToStatement)})
    returning "id", "city", "country"
    """
    .executeInsert(AddressRow.rowParser(1).single)
  }

  def insert(unsaved: AddressRowUnsaved)(using c: Connection): AddressRow = {
    val namedParameters = List(
      Some((NamedParameter("city", ParameterValue(unsaved.city, null, ToStatement.stringToStatement)), "")),
      Some((NamedParameter("country", ParameterValue(unsaved.country, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, AddressId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."address" default values
      returning "id", "city", "country"
      """
        .executeInsert(AddressRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."address"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "city", "country"
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(AddressRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[AddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."address"("id", "city", "country") FROM STDIN""", batchSize, unsaved)(using AddressRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[AddressRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."address"("city", "country", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using AddressRowUnsaved.pgText, c)

  def select: SelectBuilder[AddressFields, AddressRow] = SelectBuilder.of(""""frontpage"."address"""", AddressFields.structure, AddressRow.rowParser)

  def selectAll(using c: Connection): List[AddressRow] = {
    SQL"""select "id", "city", "country"
    from "frontpage"."address"
    """.as(AddressRow.rowParser(1).*)
  }

  def selectById(id: AddressId)(using c: Connection): Option[AddressRow] = {
    SQL"""select "id", "city", "country"
    from "frontpage"."address"
    where "id" = ${ParameterValue(id, null, AddressId.toStatement)}
    """.as(AddressRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[AddressId])(using c: Connection): List[AddressRow] = {
    SQL"""select "id", "city", "country"
    from "frontpage"."address"
    where "id" = ANY(${ParameterValue(ids, null, AddressId.arrayToStatement)})
    """.as(AddressRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[AddressId])(using c: Connection): Map[AddressId, AddressRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[AddressFields, AddressRow] = UpdateBuilder.of(""""frontpage"."address"""", AddressFields.structure, AddressRow.rowParser(1).*)

  def update(row: AddressRow)(using c: Connection): Option[AddressRow] = {
    val id = row.id
    SQL"""update "frontpage"."address"
    set "city" = ${ParameterValue(row.city, null, ToStatement.stringToStatement)},
    "country" = ${ParameterValue(row.country, null, ToStatement.stringToStatement)}
    where "id" = ${ParameterValue(id, null, AddressId.toStatement)}
    returning "id", "city", "country"
    """.executeInsert(AddressRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: AddressRow)(using c: Connection): AddressRow = {
  SQL"""insert into "frontpage"."address"("id", "city", "country")
    values (
      ${ParameterValue(unsaved.id, null, AddressId.toStatement)}::uuid,
    ${ParameterValue(unsaved.city, null, ToStatement.stringToStatement)},
    ${ParameterValue(unsaved.country, null, ToStatement.stringToStatement)}
    )
    on conflict ("id")
    do update set
      "city" = EXCLUDED."city",
    "country" = EXCLUDED."country"
    returning "id", "city", "country"
    """
    .executeInsert(AddressRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[AddressRow])(using c: Connection): List[AddressRow] = {
    def toNamedParameter(row: AddressRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, AddressId.toStatement)),
      NamedParameter("city", ParameterValue(row.city, null, ToStatement.stringToStatement)),
      NamedParameter("country", ParameterValue(row.country, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."address"("id", "city", "country")
            values ({id}::uuid, {city}, {country})
            on conflict ("id")
            do update set
              "city" = EXCLUDED."city",
            "country" = EXCLUDED."country"
            returning "id", "city", "country"
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(AddressRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[AddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table address_TEMP (like "frontpage"."address") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy address_TEMP("id", "city", "country") from stdin""", batchSize, unsaved)(using AddressRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."address"("id", "city", "country")
    select * from address_TEMP
    on conflict ("id")
    do update set
      "city" = EXCLUDED."city",
    "country" = EXCLUDED."country"
    ;
    drop table address_TEMP;""".executeUpdate()
  }
}