/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.address

import frontpage.Text
import frontpage.customtypes.Defaulted
import frontpage.customtypes.Defaulted.UseDefault
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `frontpage.address` which has not been persisted yet */
case class AddressRowUnsaved(
  city: String,
  country: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[AddressId] = new UseDefault()
) {
  def toRow(idDefault: => AddressId): AddressRow = new AddressRow(id = id.getOrElse(idDefault), city = city, country = country)
}

object AddressRowUnsaved {
  given pgText: Text[AddressRowUnsaved] = {
    Text.instance[AddressRowUnsaved]{ (row, sb) =>
      Text.stringInstance.unsafeEncode(row.city, sb)
      sb.append(Text.DELIMETER)
      Text.stringInstance.unsafeEncode(row.country, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using AddressId.pgText).unsafeEncode(row.id, sb)
    }
  }

  given reads: Reads[AddressRowUnsaved] = {
    Reads[AddressRowUnsaved](json => JsResult.fromTry(
        Try(
          AddressRowUnsaved(
            city = json.\("city").as(Reads.StringReads),
            country = json.\("country").as(Reads.StringReads),
            id = json.\("id").as(Defaulted.reads(using AddressId.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[AddressRowUnsaved] = {
    OWrites[AddressRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "city" -> Writes.StringWrites.writes(o.city),
        "country" -> Writes.StringWrites.writes(o.country),
        "id" -> Defaulted.writes(using AddressId.writes).writes(o.id)
      ))
    )
  }
}