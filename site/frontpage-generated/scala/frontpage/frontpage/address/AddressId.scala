/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.address

import anorm.Column
import anorm.ParameterMetaData
import anorm.ToStatement
import frontpage.Text
import frontpage.customtypes.TypoUUID
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import typo.dsl.Bijection

/** Type for the primary key of table `frontpage.address` */
case class AddressId(value: TypoUUID) extends scala.AnyVal

object AddressId {
  given arrayColumn: Column[Array[AddressId]] = Column.columnToArray(using column, implicitly)

  given arrayToStatement: ToStatement[Array[AddressId]] = TypoUUID.arrayToStatement.contramap(_.map(_.value))

  given bijection: Bijection[AddressId, TypoUUID] = Bijection.apply[AddressId, TypoUUID](_.value)(AddressId.apply)

  given column: Column[AddressId] = TypoUUID.column.map(AddressId.apply)

  given parameterMetadata: ParameterMetaData[AddressId] = {
    new ParameterMetaData[AddressId] {
      override def sqlType: String = TypoUUID.parameterMetadata.sqlType
      override def jdbcType: Int = TypoUUID.parameterMetadata.jdbcType
    }
  }

  given pgText: Text[AddressId] = {
    new Text[AddressId] {
      override def unsafeEncode(v: AddressId, sb: StringBuilder): Unit = TypoUUID.pgText.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: AddressId, sb: StringBuilder): Unit = TypoUUID.pgText.unsafeArrayEncode(v.value, sb)
    }
  }

  given reads: Reads[AddressId] = TypoUUID.reads.map(AddressId.apply)

  given toStatement: ToStatement[AddressId] = TypoUUID.toStatement.contramap(_.value)

  given writes: Writes[AddressId] = TypoUUID.writes.contramap(_.value)
}