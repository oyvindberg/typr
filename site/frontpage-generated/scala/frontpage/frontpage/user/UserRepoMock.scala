/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.user

import frontpage.frontpage.Email
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class UserRepoMock(
  toRow: UserRowUnsaved => UserRow,
  map: scala.collection.mutable.Map[UserId, UserRow] = scala.collection.mutable.Map.empty[UserId, UserRow]
) extends UserRepo {
  def delete: DeleteBuilder[UserFields, UserRow] = DeleteBuilderMock(DeleteParams.empty, UserFields.structure, map)

  def deleteById(id: UserId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[UserId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: UserRow)(using c: Connection): UserRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: UserRowUnsaved)(using c: Connection): UserRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[UserRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[UserRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[UserFields, UserRow] = SelectBuilderMock(UserFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[UserRow] = map.values.toList

  def selectById(id: UserId)(using c: Connection): Option[UserRow] = map.get(id)

  def selectByIds(ids: Array[UserId])(using c: Connection): List[UserRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[UserId])(using c: Connection): Map[UserId, UserRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniqueEmail(email: Email)(using c: Connection): Option[UserRow] = map.values.find(v => email == v.email)

  def update: UpdateBuilder[UserFields, UserRow] = UpdateBuilderMock(UpdateParams.empty, UserFields.structure, map)

  def update(row: UserRow)(using c: Connection): Option[UserRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: UserRow)(using c: Connection): UserRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[UserRow])(using c: Connection): List[UserRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[UserRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}