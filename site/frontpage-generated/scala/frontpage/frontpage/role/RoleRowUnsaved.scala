/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.role

import frontpage.Text
import frontpage.customtypes.Defaulted
import frontpage.customtypes.Defaulted.UseDefault
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `frontpage.role` which has not been persisted yet */
case class RoleRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[RoleId] = new UseDefault()
) {
  def toRow(idDefault: => RoleId): RoleRow = new RoleRow(id = id.getOrElse(idDefault), name = name)
}

object RoleRowUnsaved {
  given pgText: Text[RoleRowUnsaved] = {
    Text.instance[RoleRowUnsaved]{ (row, sb) =>
      Text.stringInstance.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using RoleId.pgText).unsafeEncode(row.id, sb)
    }
  }

  given reads: Reads[RoleRowUnsaved] = {
    Reads[RoleRowUnsaved](json => JsResult.fromTry(
        Try(
          RoleRowUnsaved(
            name = json.\("name").as(Reads.StringReads),
            id = json.\("id").as(Defaulted.reads(using RoleId.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[RoleRowUnsaved] = {
    OWrites[RoleRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "name" -> Writes.StringWrites.writes(o.name),
        "id" -> Defaulted.writes(using RoleId.writes).writes(o.id)
      ))
    )
  }
}