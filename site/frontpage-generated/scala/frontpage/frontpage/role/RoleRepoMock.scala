/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.role

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class RoleRepoMock(
  toRow: RoleRowUnsaved => RoleRow,
  map: scala.collection.mutable.Map[RoleId, RoleRow] = scala.collection.mutable.Map.empty[RoleId, RoleRow]
) extends RoleRepo {
  def delete: DeleteBuilder[RoleFields, RoleRow] = DeleteBuilderMock(DeleteParams.empty, RoleFields.structure, map)

  def deleteById(id: RoleId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[RoleId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: RoleRow)(using c: Connection): RoleRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: RoleRowUnsaved)(using c: Connection): RoleRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[RoleRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[RoleRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[RoleFields, RoleRow] = SelectBuilderMock(RoleFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[RoleRow] = map.values.toList

  def selectById(id: RoleId)(using c: Connection): Option[RoleRow] = map.get(id)

  def selectByIds(ids: Array[RoleId])(using c: Connection): List[RoleRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[RoleId])(using c: Connection): Map[RoleId, RoleRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniqueName(name: String)(using c: Connection): Option[RoleRow] = map.values.find(v => name == v.name)

  def update: UpdateBuilder[RoleFields, RoleRow] = UpdateBuilderMock(UpdateParams.empty, RoleFields.structure, map)

  def update(row: RoleRow)(using c: Connection): Option[RoleRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: RoleRow)(using c: Connection): RoleRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[RoleRow])(using c: Connection): List[RoleRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[RoleRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}