/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.role

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class RoleRepoImpl extends RoleRepo {
  def delete: DeleteBuilder[RoleFields, RoleRow] = DeleteBuilder.of(""""frontpage"."role"""", RoleFields.structure, RoleRow.rowParser(1).*)

  def deleteById(id: RoleId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."role" where "id" = ${ParameterValue(id, null, RoleId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[RoleId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."role"
    where "id" = ANY(${ParameterValue(ids, null, RoleId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: RoleRow)(using c: Connection): RoleRow = {
  SQL"""insert into "frontpage"."role"("id", "name")
    values (${ParameterValue(unsaved.id, null, RoleId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
    returning "id", "name"
    """
    .executeInsert(RoleRow.rowParser(1).single)
  }

  def insert(unsaved: RoleRowUnsaved)(using c: Connection): RoleRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, RoleId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."role" default values
      returning "id", "name"
      """
        .executeInsert(RoleRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."role"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "name"
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(RoleRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[RoleRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."role"("id", "name") FROM STDIN""", batchSize, unsaved)(using RoleRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[RoleRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."role"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using RoleRowUnsaved.pgText, c)

  def select: SelectBuilder[RoleFields, RoleRow] = SelectBuilder.of(""""frontpage"."role"""", RoleFields.structure, RoleRow.rowParser)

  def selectAll(using c: Connection): List[RoleRow] = {
    SQL"""select "id", "name"
    from "frontpage"."role"
    """.as(RoleRow.rowParser(1).*)
  }

  def selectById(id: RoleId)(using c: Connection): Option[RoleRow] = {
    SQL"""select "id", "name"
    from "frontpage"."role"
    where "id" = ${ParameterValue(id, null, RoleId.toStatement)}
    """.as(RoleRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[RoleId])(using c: Connection): List[RoleRow] = {
    SQL"""select "id", "name"
    from "frontpage"."role"
    where "id" = ANY(${ParameterValue(ids, null, RoleId.arrayToStatement)})
    """.as(RoleRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[RoleId])(using c: Connection): Map[RoleId, RoleRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def selectByUniqueName(name: String)(using c: Connection): Option[RoleRow] = {
    SQL"""select "id", "name"
    from "frontpage"."role"
    where "name" = ${ParameterValue(name, null, ToStatement.stringToStatement)}
    """.as(RoleRow.rowParser(1).singleOpt)
  }

  def update: UpdateBuilder[RoleFields, RoleRow] = UpdateBuilder.of(""""frontpage"."role"""", RoleFields.structure, RoleRow.rowParser(1).*)

  def update(row: RoleRow)(using c: Connection): Option[RoleRow] = {
    val id = row.id
    SQL"""update "frontpage"."role"
    set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
    where "id" = ${ParameterValue(id, null, RoleId.toStatement)}
    returning "id", "name"
    """.executeInsert(RoleRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: RoleRow)(using c: Connection): RoleRow = {
  SQL"""insert into "frontpage"."role"("id", "name")
    values (
      ${ParameterValue(unsaved.id, null, RoleId.toStatement)}::uuid,
    ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
    )
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    returning "id", "name"
    """
    .executeInsert(RoleRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[RoleRow])(using c: Connection): List[RoleRow] = {
    def toNamedParameter(row: RoleRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, RoleId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."role"("id", "name")
            values ({id}::uuid, {name})
            on conflict ("id")
            do update set
              "name" = EXCLUDED."name"
            returning "id", "name"
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(RoleRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[RoleRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table role_TEMP (like "frontpage"."role") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy role_TEMP("id", "name") from stdin""", batchSize, unsaved)(using RoleRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."role"("id", "name")
    select * from role_TEMP
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    ;
    drop table role_TEMP;""".executeUpdate()
  }
}