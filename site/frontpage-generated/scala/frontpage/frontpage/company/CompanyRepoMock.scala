/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.company

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class CompanyRepoMock(
  toRow: CompanyRowUnsaved => CompanyRow,
  map: scala.collection.mutable.Map[CompanyId, CompanyRow] = scala.collection.mutable.Map.empty[CompanyId, CompanyRow]
) extends CompanyRepo {
  def delete: DeleteBuilder[CompanyFields, CompanyRow] = DeleteBuilderMock(DeleteParams.empty, CompanyFields.structure, map)

  def deleteById(id: CompanyId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[CompanyId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: CompanyRow)(using c: Connection): CompanyRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: CompanyRowUnsaved)(using c: Connection): CompanyRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[CompanyRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[CompanyRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[CompanyFields, CompanyRow] = SelectBuilderMock(CompanyFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[CompanyRow] = map.values.toList

  def selectById(id: CompanyId)(using c: Connection): Option[CompanyRow] = map.get(id)

  def selectByIds(ids: Array[CompanyId])(using c: Connection): List[CompanyRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[CompanyId])(using c: Connection): Map[CompanyId, CompanyRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[CompanyFields, CompanyRow] = UpdateBuilderMock(UpdateParams.empty, CompanyFields.structure, map)

  def update(row: CompanyRow)(using c: Connection): Option[CompanyRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: CompanyRow)(using c: Connection): CompanyRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[CompanyRow])(using c: Connection): List[CompanyRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[CompanyRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}