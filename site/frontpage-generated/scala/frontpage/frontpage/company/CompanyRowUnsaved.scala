/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.company

import frontpage.Text
import frontpage.customtypes.Defaulted
import frontpage.customtypes.Defaulted.UseDefault
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `frontpage.company` which has not been persisted yet */
case class CompanyRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[CompanyId] = new UseDefault()
) {
  def toRow(idDefault: => CompanyId): CompanyRow = new CompanyRow(id = id.getOrElse(idDefault), name = name)
}

object CompanyRowUnsaved {
  given pgText: Text[CompanyRowUnsaved] = {
    Text.instance[CompanyRowUnsaved]{ (row, sb) =>
      Text.stringInstance.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using CompanyId.pgText).unsafeEncode(row.id, sb)
    }
  }

  given reads: Reads[CompanyRowUnsaved] = {
    Reads[CompanyRowUnsaved](json => JsResult.fromTry(
        Try(
          CompanyRowUnsaved(
            name = json.\("name").as(Reads.StringReads),
            id = json.\("id").as(Defaulted.reads(using CompanyId.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[CompanyRowUnsaved] = {
    OWrites[CompanyRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "name" -> Writes.StringWrites.writes(o.name),
        "id" -> Defaulted.writes(using CompanyId.writes).writes(o.id)
      ))
    )
  }
}