/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.department

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class DepartmentRepoMock(
  toRow: DepartmentRowUnsaved => DepartmentRow,
  map: scala.collection.mutable.Map[DepartmentId, DepartmentRow] = scala.collection.mutable.Map.empty[DepartmentId, DepartmentRow]
) extends DepartmentRepo {
  def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = DeleteBuilderMock(DeleteParams.empty, DepartmentFields.structure, map)

  def deleteById(id: DepartmentId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[DepartmentId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: DepartmentRow)(using c: Connection): DepartmentRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: DepartmentRowUnsaved)(using c: Connection): DepartmentRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[DepartmentRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[DepartmentRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[DepartmentFields, DepartmentRow] = SelectBuilderMock(DepartmentFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[DepartmentRow] = map.values.toList

  def selectById(id: DepartmentId)(using c: Connection): Option[DepartmentRow] = map.get(id)

  def selectByIds(ids: Array[DepartmentId])(using c: Connection): List[DepartmentRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[DepartmentId])(using c: Connection): Map[DepartmentId, DepartmentRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[DepartmentFields, DepartmentRow] = UpdateBuilderMock(UpdateParams.empty, DepartmentFields.structure, map)

  def update(row: DepartmentRow)(using c: Connection): Option[DepartmentRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: DepartmentRow)(using c: Connection): DepartmentRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[DepartmentRow])(using c: Connection): List[DepartmentRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[DepartmentRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}