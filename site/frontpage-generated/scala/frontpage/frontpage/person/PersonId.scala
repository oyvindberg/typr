/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.person

import anorm.Column
import anorm.ParameterMetaData
import anorm.ToStatement
import frontpage.Text
import frontpage.customtypes.TypoUUID
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import typo.dsl.Bijection

/** Type for the primary key of table `frontpage.person` */
case class PersonId(value: TypoUUID) extends scala.AnyVal

object PersonId {
  given arrayColumn: Column[Array[PersonId]] = Column.columnToArray(using column, implicitly)

  given arrayToStatement: ToStatement[Array[PersonId]] = TypoUUID.arrayToStatement.contramap(_.map(_.value))

  given bijection: Bijection[PersonId, TypoUUID] = Bijection.apply[PersonId, TypoUUID](_.value)(PersonId.apply)

  given column: Column[PersonId] = TypoUUID.column.map(PersonId.apply)

  given parameterMetadata: ParameterMetaData[PersonId] = {
    new ParameterMetaData[PersonId] {
      override def sqlType: String = TypoUUID.parameterMetadata.sqlType
      override def jdbcType: Int = TypoUUID.parameterMetadata.jdbcType
    }
  }

  given pgText: Text[PersonId] = {
    new Text[PersonId] {
      override def unsafeEncode(v: PersonId, sb: StringBuilder): Unit = TypoUUID.pgText.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: PersonId, sb: StringBuilder): Unit = TypoUUID.pgText.unsafeArrayEncode(v.value, sb)
    }
  }

  given reads: Reads[PersonId] = TypoUUID.reads.map(PersonId.apply)

  given toStatement: ToStatement[PersonId] = TypoUUID.toStatement.contramap(_.value)

  given writes: Writes[PersonId] = TypoUUID.writes.contramap(_.value)
}