/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.person

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.customtypes.TypoLocalDateTime
import frontpage.frontpage.address.AddressId
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class PersonRepoImpl extends PersonRepo {
  def delete: DeleteBuilder[PersonFields, PersonRow] = DeleteBuilder.of(""""frontpage"."person"""", PersonFields.structure, PersonRow.rowParser(1).*)

  def deleteById(id: PersonId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."person" where "id" = ${ParameterValue(id, null, PersonId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[PersonId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."person"
    where "id" = ANY(${ParameterValue(ids, null, PersonId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: PersonRow)(using c: Connection): PersonRow = {
  SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
    values (${ParameterValue(unsaved.id, null, PersonId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}, ${ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(using AddressId.toStatement, AddressId.parameterMetadata))}::uuid, ${ParameterValue(unsaved.createdAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp)
    returning "id", "name", "address_id", "created_at"::text
    """
    .executeInsert(PersonRow.rowParser(1).single)
  }

  def insert(unsaved: PersonRowUnsaved)(using c: Connection): PersonRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      Some((NamedParameter("address_id", ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(using AddressId.toStatement, AddressId.parameterMetadata))), "::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, PersonId.toStatement)), "::uuid"))
      },
      unsaved.createdAt match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("created_at", ParameterValue(value, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))), "::timestamp"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."person" default values
      returning "id", "name", "address_id", "created_at"::text
      """
        .executeInsert(PersonRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."person"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "name", "address_id", "created_at"::text
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(PersonRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[PersonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."person"("id", "name", "address_id", "created_at") FROM STDIN""", batchSize, unsaved)(using PersonRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[PersonRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."person"("name", "address_id", "id", "created_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using PersonRowUnsaved.pgText, c)

  def select: SelectBuilder[PersonFields, PersonRow] = SelectBuilder.of(""""frontpage"."person"""", PersonFields.structure, PersonRow.rowParser)

  def selectAll(using c: Connection): List[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
    from "frontpage"."person"
    """.as(PersonRow.rowParser(1).*)
  }

  def selectById(id: PersonId)(using c: Connection): Option[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
    from "frontpage"."person"
    where "id" = ${ParameterValue(id, null, PersonId.toStatement)}
    """.as(PersonRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[PersonId])(using c: Connection): List[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
    from "frontpage"."person"
    where "id" = ANY(${ParameterValue(ids, null, PersonId.arrayToStatement)})
    """.as(PersonRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[PersonId])(using c: Connection): Map[PersonId, PersonRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[PersonFields, PersonRow] = UpdateBuilder.of(""""frontpage"."person"""", PersonFields.structure, PersonRow.rowParser(1).*)

  def update(row: PersonRow)(using c: Connection): Option[PersonRow] = {
    val id = row.id
    SQL"""update "frontpage"."person"
    set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)},
    "address_id" = ${ParameterValue(row.addressId, null, ToStatement.optionToStatement(using AddressId.toStatement, AddressId.parameterMetadata))}::uuid,
    "created_at" = ${ParameterValue(row.createdAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    where "id" = ${ParameterValue(id, null, PersonId.toStatement)}
    returning "id", "name", "address_id", "created_at"::text
    """.executeInsert(PersonRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: PersonRow)(using c: Connection): PersonRow = {
  SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
    values (
      ${ParameterValue(unsaved.id, null, PersonId.toStatement)}::uuid,
    ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)},
    ${ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(using AddressId.toStatement, AddressId.parameterMetadata))}::uuid,
    ${ParameterValue(unsaved.createdAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
    )
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name",
    "address_id" = EXCLUDED."address_id",
    "created_at" = EXCLUDED."created_at"
    returning "id", "name", "address_id", "created_at"::text
    """
    .executeInsert(PersonRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[PersonRow])(using c: Connection): List[PersonRow] = {
    def toNamedParameter(row: PersonRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, PersonId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement)),
      NamedParameter("address_id", ParameterValue(row.addressId, null, ToStatement.optionToStatement(using AddressId.toStatement, AddressId.parameterMetadata))),
      NamedParameter("created_at", ParameterValue(row.createdAt, null, ToStatement.optionToStatement(using TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
            values ({id}::uuid, {name}, {address_id}::uuid, {created_at}::timestamp)
            on conflict ("id")
            do update set
              "name" = EXCLUDED."name",
            "address_id" = EXCLUDED."address_id",
            "created_at" = EXCLUDED."created_at"
            returning "id", "name", "address_id", "created_at"::text
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(PersonRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[PersonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table person_TEMP (like "frontpage"."person") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy person_TEMP("id", "name", "address_id", "created_at") from stdin""", batchSize, unsaved)(using PersonRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
    select * from person_TEMP
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name",
    "address_id" = EXCLUDED."address_id",
    "created_at" = EXCLUDED."created_at"
    ;
    drop table person_TEMP;""".executeUpdate()
  }
}