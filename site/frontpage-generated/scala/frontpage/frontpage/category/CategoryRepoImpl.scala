/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.category

import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.ToStatement
import frontpage.customtypes.Defaulted
import frontpage.streamingInsert
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class CategoryRepoImpl extends CategoryRepo {
  def delete: DeleteBuilder[CategoryFields, CategoryRow] = DeleteBuilder.of(""""frontpage"."category"""", CategoryFields.structure, CategoryRow.rowParser(1).*)

  def deleteById(id: CategoryId)(using c: Connection): Boolean = SQL"""delete from "frontpage"."category" where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}""".executeUpdate() > 0

  def deleteByIds(ids: Array[CategoryId])(using c: Connection): Int = {
    SQL"""delete
    from "frontpage"."category"
    where "id" = ANY(${ParameterValue(ids, null, CategoryId.arrayToStatement)})
    """.executeUpdate()
  }

  def insert(unsaved: CategoryRow)(using c: Connection): CategoryRow = {
  SQL"""insert into "frontpage"."category"("id", "name")
    values (${ParameterValue(unsaved.id, null, CategoryId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
    returning "id", "name"
    """
    .executeInsert(CategoryRow.rowParser(1).single)
  }

  def insert(unsaved: CategoryRowUnsaved)(using c: Connection): CategoryRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, CategoryId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."category" default values
      returning "id", "name"
      """
        .executeInsert(CategoryRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."category"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "id", "name"
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(CategoryRow.rowParser(1).single)
    }
  }

  def insertStreaming(
    unsaved: Iterator[CategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."category"("id", "name") FROM STDIN""", batchSize, unsaved)(using CategoryRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[CategoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "frontpage"."category"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using CategoryRowUnsaved.pgText, c)

  def select: SelectBuilder[CategoryFields, CategoryRow] = SelectBuilder.of(""""frontpage"."category"""", CategoryFields.structure, CategoryRow.rowParser)

  def selectAll(using c: Connection): List[CategoryRow] = {
    SQL"""select "id", "name"
    from "frontpage"."category"
    """.as(CategoryRow.rowParser(1).*)
  }

  def selectById(id: CategoryId)(using c: Connection): Option[CategoryRow] = {
    SQL"""select "id", "name"
    from "frontpage"."category"
    where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}
    """.as(CategoryRow.rowParser(1).singleOpt)
  }

  def selectByIds(ids: Array[CategoryId])(using c: Connection): List[CategoryRow] = {
    SQL"""select "id", "name"
    from "frontpage"."category"
    where "id" = ANY(${ParameterValue(ids, null, CategoryId.arrayToStatement)})
    """.as(CategoryRow.rowParser(1).*)
  }

  def selectByIdsTracked(ids: Array[CategoryId])(using c: Connection): Map[CategoryId, CategoryRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[CategoryFields, CategoryRow] = UpdateBuilder.of(""""frontpage"."category"""", CategoryFields.structure, CategoryRow.rowParser(1).*)

  def update(row: CategoryRow)(using c: Connection): Option[CategoryRow] = {
    val id = row.id
    SQL"""update "frontpage"."category"
    set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
    where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}
    returning "id", "name"
    """.executeInsert(CategoryRow.rowParser(1).singleOpt)
  }

  def upsert(unsaved: CategoryRow)(using c: Connection): CategoryRow = {
  SQL"""insert into "frontpage"."category"("id", "name")
    values (
      ${ParameterValue(unsaved.id, null, CategoryId.toStatement)}::uuid,
    ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
    )
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    returning "id", "name"
    """
    .executeInsert(CategoryRow.rowParser(1).single)
  }

  def upsertBatch(unsaved: Iterable[CategoryRow])(using c: Connection): List[CategoryRow] = {
    def toNamedParameter(row: CategoryRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, CategoryId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.frontpage.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."category"("id", "name")
            values ({id}::uuid, {name})
            on conflict ("id")
            do update set
              "name" = EXCLUDED."name"
            returning "id", "name"
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(CategoryRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[CategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table category_TEMP (like "frontpage"."category") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy category_TEMP("id", "name") from stdin""", batchSize, unsaved)(using CategoryRow.pgText, c): @nowarn
    SQL"""insert into "frontpage"."category"("id", "name")
    select * from category_TEMP
    on conflict ("id")
    do update set
      "name" = EXCLUDED."name"
    ;
    drop table category_TEMP;""".executeUpdate()
  }
}