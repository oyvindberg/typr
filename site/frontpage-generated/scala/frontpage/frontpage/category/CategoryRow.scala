/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.category

import anorm.Column
import anorm.RowParser
import anorm.Success
import frontpage.Text
import frontpage.customtypes.Defaulted
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: frontpage.category
 * Primary key: id
 */
case class CategoryRow(
  /** Default: gen_random_uuid() */
  id: CategoryId,
  name: String
) {
  def toUnsavedRow(id: Defaulted[CategoryId]): CategoryRowUnsaved = new CategoryRowUnsaved(name, id)
}

object CategoryRow {
  given pgText: Text[CategoryRow] = {
    Text.instance[CategoryRow]{ (row, sb) =>
      CategoryId.pgText.unsafeEncode(row.id, sb)
      sb.append(Text.DELIMETER)
      Text.stringInstance.unsafeEncode(row.name, sb)
    }
  }

  given reads: Reads[CategoryRow] = {
    Reads[CategoryRow](json => JsResult.fromTry(
        Try(
          CategoryRow(
            id = json.\("id").as(CategoryId.reads),
            name = json.\("name").as(Reads.StringReads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[CategoryRow] = {
    RowParser[CategoryRow] { row =>
      Success(
        CategoryRow(
          id = row(idx + 0)(using CategoryId.column),
          name = row(idx + 1)(using Column.columnToString)
        )
      )
    }
  }

  given writes: OWrites[CategoryRow] = {
    OWrites[CategoryRow](o =>
      new JsObject(ListMap[String, JsValue](
        "id" -> CategoryId.writes.writes(o.id),
        "name" -> Writes.StringWrites.writes(o.name)
      ))
    )
  }
}