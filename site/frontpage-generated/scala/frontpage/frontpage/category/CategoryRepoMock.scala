/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package frontpage.frontpage.category

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class CategoryRepoMock(
  toRow: CategoryRowUnsaved => CategoryRow,
  map: scala.collection.mutable.Map[CategoryId, CategoryRow] = scala.collection.mutable.Map.empty[CategoryId, CategoryRow]
) extends CategoryRepo {
  def delete: DeleteBuilder[CategoryFields, CategoryRow] = DeleteBuilderMock(DeleteParams.empty, CategoryFields.structure, map)

  def deleteById(id: CategoryId)(using c: Connection): Boolean = map.remove(id).isDefined

  def deleteByIds(ids: Array[CategoryId])(using c: Connection): Int = ids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: CategoryRow)(using c: Connection): CategoryRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
  
    unsaved
  }

  def insert(unsaved: CategoryRowUnsaved)(using c: Connection): CategoryRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[CategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[CategoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[CategoryFields, CategoryRow] = SelectBuilderMock(CategoryFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[CategoryRow] = map.values.toList

  def selectById(id: CategoryId)(using c: Connection): Option[CategoryRow] = map.get(id)

  def selectByIds(ids: Array[CategoryId])(using c: Connection): List[CategoryRow] = ids.flatMap(map.get).toList

  def selectByIdsTracked(ids: Array[CategoryId])(using c: Connection): Map[CategoryId, CategoryRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[CategoryFields, CategoryRow] = UpdateBuilderMock(UpdateParams.empty, CategoryFields.structure, map)

  def update(row: CategoryRow)(using c: Connection): Option[CategoryRow] = {
    map.get(row.id).map { _ =>
      map.put(row.id, row): @nowarn
      row
    }
  }

  def upsert(unsaved: CategoryRow)(using c: Connection): CategoryRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[CategoryRow])(using c: Connection): List[CategoryRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[CategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}