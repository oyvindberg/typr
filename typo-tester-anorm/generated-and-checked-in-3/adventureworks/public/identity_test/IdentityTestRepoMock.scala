/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.identity_test

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class IdentityTestRepoMock(
  toRow: IdentityTestRowUnsaved => IdentityTestRow,
  map: scala.collection.mutable.Map[IdentityTestId, IdentityTestRow] = scala.collection.mutable.Map.empty[IdentityTestId, IdentityTestRow]
) extends IdentityTestRepo {
  def delete: DeleteBuilder[IdentityTestFields, IdentityTestRow] = DeleteBuilderMock(DeleteParams.empty, IdentityTestFields.structure, map)

  def deleteById(name: IdentityTestId)(using c: Connection): Boolean = map.remove(name).isDefined

  def deleteByIds(names: Array[IdentityTestId])(using c: Connection): Int = names.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: IdentityTestRow)(using c: Connection): IdentityTestRow = {
    val _ = if (map.contains(unsaved.name))
      sys.error(s"id ${unsaved.name} already exists")
    else
      map.put(unsaved.name, unsaved)
  
    unsaved
  }

  def insert(unsaved: IdentityTestRowUnsaved)(using c: Connection): IdentityTestRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[IdentityTestRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.name -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[IdentityTestRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.name -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[IdentityTestFields, IdentityTestRow] = SelectBuilderMock(IdentityTestFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[IdentityTestRow] = map.values.toList

  def selectById(name: IdentityTestId)(using c: Connection): Option[IdentityTestRow] = map.get(name)

  def selectByIds(names: Array[IdentityTestId])(using c: Connection): List[IdentityTestRow] = names.flatMap(map.get).toList

  def selectByIdsTracked(names: Array[IdentityTestId])(using c: Connection): Map[IdentityTestId, IdentityTestRow] = {
    val byId = selectByIds(names).view.map(x => (x.name, x)).toMap
    names.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[IdentityTestFields, IdentityTestRow] = UpdateBuilderMock(UpdateParams.empty, IdentityTestFields.structure, map)

  def update(row: IdentityTestRow)(using c: Connection): Option[IdentityTestRow] = {
    map.get(row.name).map { _ =>
      map.put(row.name, row): @nowarn
      row
    }
  }

  def upsert(unsaved: IdentityTestRow)(using c: Connection): IdentityTestRow = {
    map.put(unsaved.name, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[IdentityTestRow])(using c: Connection): List[IdentityTestRow] = {
    unsaved.map { row =>
      map += (row.name -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[IdentityTestRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.name -> row)
    }
    unsaved.size
  }
}