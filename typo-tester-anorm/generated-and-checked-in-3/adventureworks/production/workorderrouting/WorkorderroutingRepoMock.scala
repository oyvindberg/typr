/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorderrouting

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class WorkorderroutingRepoMock(
  toRow: WorkorderroutingRowUnsaved => WorkorderroutingRow,
  map: scala.collection.mutable.Map[WorkorderroutingId, WorkorderroutingRow] = scala.collection.mutable.Map.empty[WorkorderroutingId, WorkorderroutingRow]
) extends WorkorderroutingRepo {
  def delete: DeleteBuilder[WorkorderroutingFields, WorkorderroutingRow] = DeleteBuilderMock(DeleteParams.empty, WorkorderroutingFields.structure, map)

  def deleteById(compositeId: WorkorderroutingId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  def deleteByIds(compositeIds: Array[WorkorderroutingId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: WorkorderroutingRow)(using c: Connection): WorkorderroutingRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  def insert(unsaved: WorkorderroutingRowUnsaved)(using c: Connection): WorkorderroutingRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[WorkorderroutingRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[WorkorderroutingRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[WorkorderroutingFields, WorkorderroutingRow] = SelectBuilderMock(WorkorderroutingFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[WorkorderroutingRow] = map.values.toList

  def selectById(compositeId: WorkorderroutingId)(using c: Connection): Option[WorkorderroutingRow] = map.get(compositeId)

  def selectByIds(compositeIds: Array[WorkorderroutingId])(using c: Connection): List[WorkorderroutingRow] = compositeIds.flatMap(map.get).toList

  def selectByIdsTracked(compositeIds: Array[WorkorderroutingId])(using c: Connection): Map[WorkorderroutingId, WorkorderroutingRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[WorkorderroutingFields, WorkorderroutingRow] = UpdateBuilderMock(UpdateParams.empty, WorkorderroutingFields.structure, map)

  def update(row: WorkorderroutingRow)(using c: Connection): Option[WorkorderroutingRow] = {
    map.get(row.compositeId).map { _ =>
      map.put(row.compositeId, row): @nowarn
      row
    }
  }

  def upsert(unsaved: WorkorderroutingRow)(using c: Connection): WorkorderroutingRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[WorkorderroutingRow])(using c: Connection): List[WorkorderroutingRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[WorkorderroutingRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}