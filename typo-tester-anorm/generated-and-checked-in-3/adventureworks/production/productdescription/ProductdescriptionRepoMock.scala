/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdescription

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class ProductdescriptionRepoMock(
  toRow: ProductdescriptionRowUnsaved => ProductdescriptionRow,
  map: scala.collection.mutable.Map[ProductdescriptionId, ProductdescriptionRow] = scala.collection.mutable.Map.empty[ProductdescriptionId, ProductdescriptionRow]
) extends ProductdescriptionRepo {
  def delete: DeleteBuilder[ProductdescriptionFields, ProductdescriptionRow] = DeleteBuilderMock(DeleteParams.empty, ProductdescriptionFields.structure, map)

  def deleteById(productdescriptionid: ProductdescriptionId)(using c: Connection): Boolean = map.remove(productdescriptionid).isDefined

  def deleteByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Int = productdescriptionids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    val _ = if (map.contains(unsaved.productdescriptionid))
      sys.error(s"id ${unsaved.productdescriptionid} already exists")
    else
      map.put(unsaved.productdescriptionid, unsaved)
  
    unsaved
  }

  def insert(unsaved: ProductdescriptionRowUnsaved)(using c: Connection): ProductdescriptionRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.productdescriptionid -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[ProductdescriptionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.productdescriptionid -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[ProductdescriptionFields, ProductdescriptionRow] = SelectBuilderMock(ProductdescriptionFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[ProductdescriptionRow] = map.values.toList

  def selectById(productdescriptionid: ProductdescriptionId)(using c: Connection): Option[ProductdescriptionRow] = map.get(productdescriptionid)

  def selectByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): List[ProductdescriptionRow] = productdescriptionids.flatMap(map.get).toList

  def selectByIdsTracked(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Map[ProductdescriptionId, ProductdescriptionRow] = {
    val byId = selectByIds(productdescriptionids).view.map(x => (x.productdescriptionid, x)).toMap
    productdescriptionids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[ProductdescriptionFields, ProductdescriptionRow] = UpdateBuilderMock(UpdateParams.empty, ProductdescriptionFields.structure, map)

  def update(row: ProductdescriptionRow)(using c: Connection): Option[ProductdescriptionRow] = {
    map.get(row.productdescriptionid).map { _ =>
      map.put(row.productdescriptionid, row): @nowarn
      row
    }
  }

  def upsert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    map.put(unsaved.productdescriptionid, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[ProductdescriptionRow])(using c: Connection): List[ProductdescriptionRow] = {
    unsaved.map { row =>
      map += (row.productdescriptionid -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.productdescriptionid -> row)
    }
    unsaved.size
  }
}