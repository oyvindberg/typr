/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class IllustrationRepoMock(
  toRow: IllustrationRowUnsaved => IllustrationRow,
  map: scala.collection.mutable.Map[IllustrationId, IllustrationRow] = scala.collection.mutable.Map.empty[IllustrationId, IllustrationRow]
) extends IllustrationRepo {
  def delete: DeleteBuilder[IllustrationFields, IllustrationRow] = DeleteBuilderMock(DeleteParams.empty, IllustrationFields.structure, map)

  def deleteById(illustrationid: IllustrationId)(using c: Connection): Boolean = map.remove(illustrationid).isDefined

  def deleteByIds(illustrationids: Array[IllustrationId])(using c: Connection): Int = illustrationids.map(id => map.remove(id)).count(_.isDefined)

  def insert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
    val _ = if (map.contains(unsaved.illustrationid))
      sys.error(s"id ${unsaved.illustrationid} already exists")
    else
      map.put(unsaved.illustrationid, unsaved)
  
    unsaved
  }

  def insert(unsaved: IllustrationRowUnsaved)(using c: Connection): IllustrationRow = insert(toRow(unsaved))

  def insertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.illustrationid -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(
    unsaved: Iterator[IllustrationRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.illustrationid -> row)
    }
    unsaved.size.toLong
  }

  def select: SelectBuilder[IllustrationFields, IllustrationRow] = SelectBuilderMock(IllustrationFields.structure, () => map.values.toList, SelectParams.empty)

  def selectAll(using c: Connection): List[IllustrationRow] = map.values.toList

  def selectById(illustrationid: IllustrationId)(using c: Connection): Option[IllustrationRow] = map.get(illustrationid)

  def selectByIds(illustrationids: Array[IllustrationId])(using c: Connection): List[IllustrationRow] = illustrationids.flatMap(map.get).toList

  def selectByIdsTracked(illustrationids: Array[IllustrationId])(using c: Connection): Map[IllustrationId, IllustrationRow] = {
    val byId = selectByIds(illustrationids).view.map(x => (x.illustrationid, x)).toMap
    illustrationids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  def update: UpdateBuilder[IllustrationFields, IllustrationRow] = UpdateBuilderMock(UpdateParams.empty, IllustrationFields.structure, map)

  def update(row: IllustrationRow)(using c: Connection): Option[IllustrationRow] = {
    map.get(row.illustrationid).map { _ =>
      map.put(row.illustrationid, row): @nowarn
      row
    }
  }

  def upsert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
    map.put(unsaved.illustrationid, unsaved): @nowarn
    unsaved
  }

  def upsertBatch(unsaved: Iterable[IllustrationRow])(using c: Connection): List[IllustrationRow] = {
    unsaved.map { row =>
      map += (row.illustrationid -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.illustrationid -> row)
    }
    unsaved.size
  }
}