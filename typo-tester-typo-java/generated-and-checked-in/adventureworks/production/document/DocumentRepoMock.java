/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.document;

import adventureworks.customtypes.TypoUUID;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record DocumentRepoMock(
  java.util.function.Function<DocumentRowUnsaved, DocumentRow> toRow,
  HashMap<DocumentId, DocumentRow> map
) implements DocumentRepo {
  public DocumentRepoMock(java.util.function.Function<DocumentRowUnsaved, DocumentRow> toRow) {
    this(toRow, new HashMap<DocumentId, DocumentRow>());
  };

  public DocumentRepoMock withToRow(java.util.function.Function<DocumentRowUnsaved, DocumentRow> toRow) {
    return new DocumentRepoMock(toRow, map);
  };

  public DocumentRepoMock withMap(HashMap<DocumentId, DocumentRow> map) {
    return new DocumentRepoMock(toRow, map);
  };

  public DeleteBuilder<DocumentFields, DocumentRow> delete() {
    return new DeleteBuilderMock<>(DocumentFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.documentnode(), id -> map.remove(id));
  };

  public Boolean deleteById(
    DocumentId documentnode,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(documentnode)).isPresent();
  };

  public Integer deleteByIds(
    DocumentId[] documentnodes,
    Connection c
  ) {
    var count = 0;
      for (var id : documentnodes) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public DocumentRow insert(
    DocumentRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.documentnode())) {
        throw new RuntimeException(str("id $unsaved.documentnode() already exists"));
      };
      map.put(unsaved.documentnode(), unsaved);
    return unsaved;
  };

  public DocumentRow insert(
    DocumentRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<DocumentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.documentnode(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<DocumentRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.documentnode(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<DocumentFields, DocumentRow> select() {
    return new SelectBuilderMock<>(DocumentFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<DocumentRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<DocumentRow> selectById(
    DocumentId documentnode,
    Connection c
  ) {
    return Optional.ofNullable(map.get(documentnode));
  };

  public List<DocumentRow> selectByIds(
    DocumentId[] documentnodes,
    Connection c
  ) {
    var result = new ArrayList<DocumentRow>();
      for (var id : documentnodes) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<DocumentId, DocumentRow> selectByIdsTracked(
    DocumentId[] documentnodes,
    Connection c
  ) {
    return selectByIds(documentnodes, c).stream().collect(Collectors.toMap((adventureworks.production.document.DocumentRow row) -> row.documentnode(), Function.identity()));
  };

  public Optional<DocumentRow> selectByUniqueRowguid(
    TypoUUID rowguid,
    Connection c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> rowguid.equals(v.rowguid())).findFirst();
  };

  public UpdateBuilder<DocumentFields, DocumentRow> update() {
    return new UpdateBuilderMock<>(DocumentFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    DocumentRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.documentnode())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.documentnode(), row);
      };
    return shouldUpdate;
  };

  public DocumentRow upsert(
    DocumentRow unsaved,
    Connection c
  ) {
    map.put(unsaved.documentnode(), unsaved);
    return unsaved;
  };

  public List<DocumentRow> upsertBatch(
    Iterator<DocumentRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<DocumentRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.documentnode(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<DocumentRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.documentnode(), row);
        count = count + 1;
      };
    return count;
  };
}