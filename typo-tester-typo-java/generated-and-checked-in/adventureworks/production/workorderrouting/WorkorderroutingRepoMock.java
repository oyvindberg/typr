/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorderrouting;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record WorkorderroutingRepoMock(
  java.util.function.Function<WorkorderroutingRowUnsaved, WorkorderroutingRow> toRow,
  HashMap<WorkorderroutingId, WorkorderroutingRow> map
) implements WorkorderroutingRepo {
  public WorkorderroutingRepoMock(java.util.function.Function<WorkorderroutingRowUnsaved, WorkorderroutingRow> toRow) {
    this(toRow, new HashMap<WorkorderroutingId, WorkorderroutingRow>());
  };

  public WorkorderroutingRepoMock withToRow(java.util.function.Function<WorkorderroutingRowUnsaved, WorkorderroutingRow> toRow) {
    return new WorkorderroutingRepoMock(toRow, map);
  };

  public WorkorderroutingRepoMock withMap(HashMap<WorkorderroutingId, WorkorderroutingRow> map) {
    return new WorkorderroutingRepoMock(toRow, map);
  };

  public DeleteBuilder<WorkorderroutingFields, WorkorderroutingRow> delete() {
    return new DeleteBuilderMock<>(WorkorderroutingFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  public Boolean deleteById(
    WorkorderroutingId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  public Integer deleteByIds(
    WorkorderroutingId[] compositeIds,
    Connection c
  ) {
    var count = 0;
      for (var id : compositeIds) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public WorkorderroutingRow insert(
    WorkorderroutingRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
        throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
      };
      map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public WorkorderroutingRow insert(
    WorkorderroutingRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<WorkorderroutingRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<WorkorderroutingRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.compositeId(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<WorkorderroutingFields, WorkorderroutingRow> select() {
    return new SelectBuilderMock<>(WorkorderroutingFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<WorkorderroutingRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<WorkorderroutingRow> selectById(
    WorkorderroutingId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  public List<WorkorderroutingRow> selectByIds(
    WorkorderroutingId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<WorkorderroutingRow>();
      for (var id : compositeIds) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<WorkorderroutingId, WorkorderroutingRow> selectByIdsTracked(
    WorkorderroutingId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((adventureworks.production.workorderrouting.WorkorderroutingRow row) -> row.compositeId(), Function.identity()));
  };

  public UpdateBuilder<WorkorderroutingFields, WorkorderroutingRow> update() {
    return new UpdateBuilderMock<>(WorkorderroutingFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    WorkorderroutingRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.compositeId(), row);
      };
    return shouldUpdate;
  };

  public WorkorderroutingRow upsert(
    WorkorderroutingRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  public List<WorkorderroutingRow> upsertBatch(
    Iterator<WorkorderroutingRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<WorkorderroutingRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<WorkorderroutingRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.compositeId(), row);
        count = count + 1;
      };
    return count;
  };
}