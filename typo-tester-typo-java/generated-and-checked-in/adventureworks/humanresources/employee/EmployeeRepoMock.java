/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employee;

import adventureworks.person.businessentity.BusinessentityId;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record EmployeeRepoMock(
  java.util.function.Function<EmployeeRowUnsaved, EmployeeRow> toRow,
  HashMap<BusinessentityId, EmployeeRow> map
) implements EmployeeRepo {
  public EmployeeRepoMock(java.util.function.Function<EmployeeRowUnsaved, EmployeeRow> toRow) {
    this(toRow, new HashMap<BusinessentityId, EmployeeRow>());
  };

  public EmployeeRepoMock withToRow(java.util.function.Function<EmployeeRowUnsaved, EmployeeRow> toRow) {
    return new EmployeeRepoMock(toRow, map);
  };

  public EmployeeRepoMock withMap(HashMap<BusinessentityId, EmployeeRow> map) {
    return new EmployeeRepoMock(toRow, map);
  };

  public DeleteBuilder<EmployeeFields, EmployeeRow> delete() {
    return new DeleteBuilderMock<>(EmployeeFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.businessentityid(), id -> map.remove(id));
  };

  public Boolean deleteById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(businessentityid)).isPresent();
  };

  public Integer deleteByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var count = 0;
      for (var id : businessentityids) { if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
      } };
    return count;
  };

  public EmployeeRow insert(
    EmployeeRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.businessentityid())) {
        throw new RuntimeException(str("id $unsaved.businessentityid() already exists"));
      };
      map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  public EmployeeRow insert(
    EmployeeRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  public Long insertStreaming(
    Iterator<EmployeeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.businessentityid(), row);
        count = count + 1L;
      };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  public Long insertUnsavedStreaming(
    Iterator<EmployeeRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next();
        var row = toRow.apply(unsavedRow);
        map.put(row.businessentityid(), row);
        count = count + 1L;
      };
    return count;
  };

  public SelectBuilder<EmployeeFields, EmployeeRow> select() {
    return new SelectBuilderMock<>(EmployeeFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  public List<EmployeeRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  public Optional<EmployeeRow> selectById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(businessentityid));
  };

  public List<EmployeeRow> selectByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var result = new ArrayList<EmployeeRow>();
      for (var id : businessentityids) { var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) result.add(opt.get()); };
    return result;
  };

  public Map<BusinessentityId, EmployeeRow> selectByIdsTracked(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    return selectByIds(businessentityids, c).stream().collect(Collectors.toMap((adventureworks.humanresources.employee.EmployeeRow row) -> row.businessentityid(), Function.identity()));
  };

  public UpdateBuilder<EmployeeFields, EmployeeRow> update() {
    return new UpdateBuilderMock<>(EmployeeFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  public Boolean update(
    EmployeeRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.businessentityid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
      if (shouldUpdate) {
        map.put(row.businessentityid(), row);
      };
    return shouldUpdate;
  };

  public EmployeeRow upsert(
    EmployeeRow unsaved,
    Connection c
  ) {
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  public List<EmployeeRow> upsertBatch(
    Iterator<EmployeeRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<EmployeeRow>();
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.businessentityid(), row);
        result.add(row);
      };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  public Integer upsertStreaming(
    Iterator<EmployeeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
      while (unsaved.hasNext()) {
        var row = unsaved.next();
        map.put(row.businessentityid(), row);
        count = count + 1;
      };
    return count;
  };
}