/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.showcase

import adventureworks.Text
import anorm.Column
import anorm.ParameterMetaData
import anorm.ToStatement
import java.sql.Types
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import typr.dsl.Bijection

/** Domain `showcase.phone_number`
 * Constraint: CHECK (((VALUE)::text ~ '^\+?[0-9\s\-\(\)]{7,20}$'::text))
 */
case class PhoneNumber(value: String)

object PhoneNumber {
  given arrayColumn: Column[Array[PhoneNumber]] = Column.columnToArray(using column, summon)

  given arrayToStatement: ToStatement[Array[PhoneNumber]] = ToStatement.arrayToParameter(using ParameterMetaData.StringParameterMetaData).contramap(_.map(_.value))

  given bijection: Bijection[PhoneNumber, String] = Bijection.apply[PhoneNumber, String](_.value)(PhoneNumber.apply)

  given column: Column[PhoneNumber] = Column.columnToString.map(PhoneNumber.apply)

  given parameterMetadata: ParameterMetaData[PhoneNumber] = {
    new ParameterMetaData[PhoneNumber] {
      override def sqlType: String = """"showcase"."phone_number""""
      override def jdbcType: Int = Types.OTHER
    }
  }

  given pgText: Text[PhoneNumber] = {
    new Text[PhoneNumber] {
      override def unsafeEncode(v: PhoneNumber, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: PhoneNumber, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given reads: Reads[PhoneNumber] = Reads.StringReads.map(PhoneNumber.apply)

  given toStatement: ToStatement[PhoneNumber] = ToStatement.stringToStatement.contramap(_.value)

  given writes: Writes[PhoneNumber] = Writes.StringWrites.contramap(_.value)
}