/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.employees

import dev.typr.foundations.dsl.DeleteBuilder
import dev.typr.foundations.dsl.DeleteBuilderMock
import dev.typr.foundations.dsl.DeleteParams
import dev.typr.foundations.dsl.SelectBuilder
import dev.typr.foundations.dsl.SelectBuilderMock
import dev.typr.foundations.dsl.SelectParams
import dev.typr.foundations.dsl.UpdateBuilder
import dev.typr.foundations.dsl.UpdateBuilderMock
import dev.typr.foundations.dsl.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors

case class EmployeesRepoMock(
  toRow: EmployeesRowUnsaved => EmployeesRow,
  map: HashMap[EmployeesId, EmployeesRow] = new HashMap[EmployeesId, EmployeesRow]()
) extends EmployeesRepo {
  override def delete: DeleteBuilder[EmployeesFields, EmployeesRow] = DeleteBuilderMock(EmployeesFields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: EmployeesId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(compositeId)).isPresent()

  override def deleteByIds(compositeIds: Array[EmployeesId])(using c: Connection): Integer = {
    var count = 0
    compositeIds.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: EmployeesRow)(using c: Connection): EmployeesId = {
    if (map.containsKey(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved.compositeId
  }

  override def insert(unsaved: EmployeesRowUnsaved)(using c: Connection): EmployeesId = insert(toRow(unsaved))(using c)

  override def select: SelectBuilder[EmployeesFields, EmployeesRow] = SelectBuilderMock(EmployeesFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[EmployeesRow] = new ArrayList(map.values())

  override def selectById(compositeId: EmployeesId)(using c: Connection): Optional[EmployeesRow] = Optional.ofNullable(map.get(compositeId))

  override def selectByIds(compositeIds: Array[EmployeesId])(using c: Connection): java.util.List[EmployeesRow] = {
    val result = new ArrayList[EmployeesRow]()
    compositeIds.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(compositeIds: Array[EmployeesId])(using c: Connection): java.util.Map[EmployeesId, EmployeesRow] = selectByIds(compositeIds)(using c).stream().collect(Collectors.toMap((row: EmployeesRow) => row.compositeId, Function.identity()))

  override def update: UpdateBuilder[EmployeesFields, EmployeesRow] = UpdateBuilderMock(EmployeesFields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def update(row: EmployeesRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.compositeId)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: EmployeesRow)(using c: Connection): Unit = map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn

  override def upsertBatch(unsaved: java.util.Iterator[EmployeesRow])(using c: Connection): Unit = {
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
  }
}