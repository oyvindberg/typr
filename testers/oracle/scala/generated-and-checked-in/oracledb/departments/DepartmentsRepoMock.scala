/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.departments

import dev.typr.foundations.dsl.DeleteBuilder
import dev.typr.foundations.dsl.DeleteBuilderMock
import dev.typr.foundations.dsl.DeleteParams
import dev.typr.foundations.dsl.SelectBuilder
import dev.typr.foundations.dsl.SelectBuilderMock
import dev.typr.foundations.dsl.SelectParams
import dev.typr.foundations.dsl.UpdateBuilder
import dev.typr.foundations.dsl.UpdateBuilderMock
import dev.typr.foundations.dsl.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors

case class DepartmentsRepoMock(map: HashMap[DepartmentsId, DepartmentsRow] = new HashMap[DepartmentsId, DepartmentsRow]()) extends DepartmentsRepo {
  override def delete: DeleteBuilder[DepartmentsFields, DepartmentsRow] = DeleteBuilderMock(DepartmentsFields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: DepartmentsId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(compositeId)).isPresent()

  override def deleteByIds(compositeIds: Array[DepartmentsId])(using c: Connection): Integer = {
    var count = 0
    compositeIds.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: DepartmentsRow)(using c: Connection): DepartmentsId = {
    if (map.containsKey(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved.compositeId
  }

  override def select: SelectBuilder[DepartmentsFields, DepartmentsRow] = SelectBuilderMock(DepartmentsFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[DepartmentsRow] = new ArrayList(map.values())

  override def selectById(compositeId: DepartmentsId)(using c: Connection): Optional[DepartmentsRow] = Optional.ofNullable(map.get(compositeId))

  override def selectByIds(compositeIds: Array[DepartmentsId])(using c: Connection): java.util.List[DepartmentsRow] = {
    val result = new ArrayList[DepartmentsRow]()
    compositeIds.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(compositeIds: Array[DepartmentsId])(using c: Connection): java.util.Map[DepartmentsId, DepartmentsRow] = selectByIds(compositeIds)(using c).stream().collect(Collectors.toMap((row: DepartmentsRow) => row.compositeId, Function.identity()))

  override def update: UpdateBuilder[DepartmentsFields, DepartmentsRow] = UpdateBuilderMock(DepartmentsFields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def update(row: DepartmentsRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.compositeId)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: DepartmentsRow)(using c: Connection): Unit = map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn

  override def upsertBatch(unsaved: java.util.Iterator[DepartmentsRow])(using c: Connection): Unit = {
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
  }
}