/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.identity_params_test

import dev.typr.foundations.scala.DeleteBuilder
import dev.typr.foundations.scala.DeleteBuilderMock
import dev.typr.foundations.scala.DeleteParams
import dev.typr.foundations.scala.SelectBuilder
import dev.typr.foundations.scala.SelectBuilderMock
import dev.typr.foundations.scala.SelectParams
import dev.typr.foundations.scala.UpdateBuilder
import dev.typr.foundations.scala.UpdateBuilderMock
import dev.typr.foundations.scala.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection

case class IdentityParamsTestRepoMock(
  toRow: IdentityParamsTestRowUnsaved => IdentityParamsTestRow,
  map: scala.collection.mutable.Map[IdentityParamsTestId, IdentityParamsTestRow] = scala.collection.mutable.Map.empty[IdentityParamsTestId, IdentityParamsTestRow]
) extends IdentityParamsTestRepo {
  override def delete: DeleteBuilder[IdentityParamsTestFields, IdentityParamsTestRow] = DeleteBuilderMock(IdentityParamsTestFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.id, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(id: IdentityParamsTestId)(using c: Connection): Boolean = map.remove(id).isDefined

  override def deleteByIds(ids: Array[IdentityParamsTestId])(using c: Connection): Int = {
    var count = 0
    ids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: IdentityParamsTestRow)(using c: Connection): IdentityParamsTestRow = {
    if (map.contains(unsaved.id)) {
      throw new RuntimeException(s"id ${unsaved.id} already exists")
    }
    map.put(unsaved.id, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: IdentityParamsTestRowUnsaved)(using c: Connection): IdentityParamsTestRow = insert(toRow(unsaved))(using c)

  override def select: SelectBuilder[IdentityParamsTestFields, IdentityParamsTestRow] = SelectBuilderMock(IdentityParamsTestFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[IdentityParamsTestRow] = map.values.toList

  override def selectById(id: IdentityParamsTestId)(using c: Connection): Option[IdentityParamsTestRow] = map.get(id)

  override def selectByIds(ids: Array[IdentityParamsTestId])(using c: Connection): List[IdentityParamsTestRow] = ids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(ids: Array[IdentityParamsTestId])(using c: Connection): Map[IdentityParamsTestId, IdentityParamsTestRow] = selectByIds(ids)(using c).map(x => (((row: IdentityParamsTestRow) => row.id).apply(x), x)).toMap

  override def update: UpdateBuilder[IdentityParamsTestFields, IdentityParamsTestRow] = UpdateBuilderMock(IdentityParamsTestFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: IdentityParamsTestRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.id).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.id, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: IdentityParamsTestRow)(using c: Connection): Unit = map.put(unsaved.id, unsaved): @scala.annotation.nowarn

  override def upsertBatch(unsaved: Iterator[IdentityParamsTestRow])(using c: Connection): Unit = {
    unsaved.foreach { row =>
      map.put(row.id, row): @scala.annotation.nowarn
    }
  }
}