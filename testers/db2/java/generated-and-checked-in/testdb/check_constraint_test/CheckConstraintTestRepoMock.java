/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.check_constraint_test;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record CheckConstraintTestRepoMock(
    HashMap<CheckConstraintTestId, CheckConstraintTestRow> map) implements CheckConstraintTestRepo {
  public CheckConstraintTestRepoMock() {
    this(new HashMap<CheckConstraintTestId, CheckConstraintTestRow>());
  }
  ;

  public CheckConstraintTestRepoMock withMap(
      HashMap<CheckConstraintTestId, CheckConstraintTestRow> map) {
    return new CheckConstraintTestRepoMock(map);
  }
  ;

  @Override
  public DeleteBuilder<CheckConstraintTestFields, CheckConstraintTestRow> delete() {
    return new DeleteBuilderMock<>(
        CheckConstraintTestFields.structure,
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.id(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(CheckConstraintTestId id, Connection c) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(CheckConstraintTestId[] ids, Connection c) {
    var count = 0;
    for (var id : ids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public CheckConstraintTestRow insert(CheckConstraintTestRow unsaved, Connection c) {
    if (map.containsKey(unsaved.id())) {
      throw new RuntimeException("id " + unsaved.id() + " already exists");
    }
    ;
    map.put(unsaved.id(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public SelectBuilder<CheckConstraintTestFields, CheckConstraintTestRow> select() {
    return new SelectBuilderMock<>(
        CheckConstraintTestFields.structure,
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<CheckConstraintTestRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<CheckConstraintTestRow> selectById(CheckConstraintTestId id, Connection c) {
    return Optional.ofNullable(map.get(id));
  }
  ;

  @Override
  public List<CheckConstraintTestRow> selectByIds(CheckConstraintTestId[] ids, Connection c) {
    var result = new ArrayList<CheckConstraintTestRow>();
    for (var id : ids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<CheckConstraintTestId, CheckConstraintTestRow> selectByIdsTracked(
      CheckConstraintTestId[] ids, Connection c) {
    return selectByIds(ids, c).stream()
        .collect(Collectors.toMap((CheckConstraintTestRow row) -> row.id(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<CheckConstraintTestFields, CheckConstraintTestRow> update() {
    return new UpdateBuilderMock<>(
        CheckConstraintTestFields.structure,
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(CheckConstraintTestRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.id(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public void upsert(CheckConstraintTestRow unsaved, Connection c) {
    map.put(unsaved.id(), unsaved);
  }
  ;

  @Override
  public void upsertBatch(Iterator<CheckConstraintTestRow> unsaved, Connection c) {
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.id(), row);
    }
    ;
  }
  ;
}
