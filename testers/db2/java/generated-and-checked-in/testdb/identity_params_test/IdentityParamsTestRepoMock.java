/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.identity_params_test;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record IdentityParamsTestRepoMock(
    java.util.function.Function<IdentityParamsTestRowUnsaved, IdentityParamsTestRow> toRow,
    HashMap<IdentityParamsTestId, IdentityParamsTestRow> map)
    implements IdentityParamsTestRepo {
  public IdentityParamsTestRepoMock(
      java.util.function.Function<IdentityParamsTestRowUnsaved, IdentityParamsTestRow> toRow) {
    this(toRow, new HashMap<IdentityParamsTestId, IdentityParamsTestRow>());
  }
  ;

  public IdentityParamsTestRepoMock withToRow(
      java.util.function.Function<IdentityParamsTestRowUnsaved, IdentityParamsTestRow> toRow) {
    return new IdentityParamsTestRepoMock(toRow, map);
  }
  ;

  public IdentityParamsTestRepoMock withMap(
      HashMap<IdentityParamsTestId, IdentityParamsTestRow> map) {
    return new IdentityParamsTestRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<IdentityParamsTestFields, IdentityParamsTestRow> delete() {
    return new DeleteBuilderMock<>(
        IdentityParamsTestFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.id(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(IdentityParamsTestId id, Connection c) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(IdentityParamsTestId[] ids, Connection c) {
    var count = 0;
    for (var id : ids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public IdentityParamsTestRow insert(IdentityParamsTestRow unsaved, Connection c) {
    if (map.containsKey(unsaved.id())) {
      throw new RuntimeException("id " + unsaved.id() + " already exists");
    }
    ;
    map.put(unsaved.id(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public IdentityParamsTestRow insert(IdentityParamsTestRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<IdentityParamsTestFields, IdentityParamsTestRow> select() {
    return new SelectBuilderMock<>(
        IdentityParamsTestFields.structure(),
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<IdentityParamsTestRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<IdentityParamsTestRow> selectById(IdentityParamsTestId id, Connection c) {
    return Optional.ofNullable(map.get(id));
  }
  ;

  @Override
  public List<IdentityParamsTestRow> selectByIds(IdentityParamsTestId[] ids, Connection c) {
    var result = new ArrayList<IdentityParamsTestRow>();
    for (var id : ids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<IdentityParamsTestId, IdentityParamsTestRow> selectByIdsTracked(
      IdentityParamsTestId[] ids, Connection c) {
    return selectByIds(ids, c).stream()
        .collect(Collectors.toMap((IdentityParamsTestRow row) -> row.id(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<IdentityParamsTestFields, IdentityParamsTestRow> update() {
    return new UpdateBuilderMock<>(
        IdentityParamsTestFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(IdentityParamsTestRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.id(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public void upsert(IdentityParamsTestRow unsaved, Connection c) {
    map.put(unsaved.id(), unsaved);
  }
  ;

  @Override
  public void upsertBatch(Iterator<IdentityParamsTestRow> unsaved, Connection c) {
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.id(), row);
    }
    ;
  }
  ;
}
